<div id=toc></div>

# Table of Contents

- [cs.NI](#cs.NI) [Total: 4]
- [cs.MA](#cs.MA) [Total: 4]
- [cs.SE](#cs.SE) [Total: 13]
- [cs.PF](#cs.PF) [Total: 1]
- [cs.DC](#cs.DC) [Total: 10]
- [cs.AR](#cs.AR) [Total: 10]


<div id='cs.NI'></div>

# cs.NI [[Back]](#toc)

### [1] [DualPI2 Active Queue Management in ns-3: Implementation And Validation](https://arxiv.org/abs/2608.12513)
*Maria Eduarda Veras,Eduardo Freitas,Assis T. de Oliveira Filho,Djamel Sadok,Judith Kelner*

Main category: cs.NI

TL;DR: 本文实现并验证了DualPI2 AQM在ns-3中的模型，确保与Linux内核一致，并通过25个BDP场景验证，模型准确再现真实行为，为L4S研究提供可靠工具。


<details>
  <summary>Details</summary>
Motivation: 缺乏高保真DualPI2模型阻碍L4S研究，需在ns-3中实现并验证。

Method: 在ns-3实现DualPI2，加入WRR、step-marking、overload protection等机制；在物理Linux测试床上进行25个BDP场景的对比验证。

Result: 模型与真实测试结果高度一致，保证吞吐公平和队列延迟隔离。

Conclusion: 该模型为研究社区提供可靠工具，可用于评估和改进L4S在不同网络拓扑中的性能。

Abstract: The demand for ultra-low latency applications necessitates advanced network architectures like the Low Latency, Low Loss, and Scalable Throughput (L4S) standard. A core component of L4S is the DualPI2 Active Queue Management (AQM), which ensures the safe coexistence of scalable and classic traffic. Despite L4S's growing adoption, the ns-3 network simulator lacks a high-fidelity, up-to-date DualPI2 model. This paper presents a comprehensive implementation of the DualPI2 AQM in ns-3, while also mirroring the official Linux Kernel architecture. Our model incorporates representative mechanisms previously absent in simulation, such as credit-based Weighted Round Robin (WRR) scheduling, step-marking, and overload protection. To guarantee simulation accuracy, we validate our implementation against a physical Linux testbed across 25 diverse Bandwidth-Delay Product (BDP) scenarios. Results demonstrate that our ns-3 model replicates real-world behavior, ensuring strict throughput fairness and queue delay isolation. Ultimately, this validated model equips the research community with a robust tool to evaluate and advance L4S performance across diverse network topologies, ranging from data centers to wireless home and office environments.

</details>


### [2] [Digital Twin Satellite Networks: A Paradigm for Intelligent, Efficient, and Resilient Operations](https://arxiv.org/abs/2608.12865)
*Mustafa Alhassan,Peng Hu*

Main category: cs.NI

TL;DR: 本文提出了一种数字孪生卫星网络（DTSN）框架，用于低地球轨道（LEO）卫星星座的可靠智能管理，通过结合实时遥测、集成感知通信（ISAC）、预测智能和韧性控制，实现对快速变化拓扑、间歇性星间链路及硬件干扰等挑战的应对。


<details>
  <summary>Details</summary>
Motivation: LEO卫星超级星座面临快速网络拓扑变化、间<think>

Method: 作者设计了一个跨域联合仿真方法，结合NASA 42航天器模拟器和基于Python的数字孪生桥构建虚实同步模型，并在600秒飞行窗口内模拟多领域威胁环境，包括运动漂移、硬件故障和对抗性阻塞。基于预测超前机制和指数传感器恢复模型，系统能够识别受损节点并触发主动的网络重组策略。

Result: 该框架成功实现了虚实同步管理，并能在面对运动漂移、硬件故障及对抗性干扰等复杂场景时，隔离受损节点并进行主动网络重配，确保服务不间断运行，提升网络动态韧性。

Conclusion: DTSN框架为预测性和面向韧性的卫星网络操作提供了有效支，有望成为下一代非地面网络智能管理的重要架构。

Abstract: Satellite mega-constellations in Low Earth Orbit (LEO) are becoming an important part of next-generation non-terrestrial networks, but their operation remains challenging because of fast network topology variation, intermittent inter-satellite links, hardware disturbances, and strict Size, Weight, and Power (SWaP) constraints. Existing approaches based on Digital Twin (DT), Digital Twin Network (DTN), Software-Defined Networking (SDN), and Open Radio Access Network (O-RAN) provide useful building blocks for intelligent satellite networking, but they do not fully support real-time, predictive, and platform-aware network operation. In this paper, we propose a Digital Twin Satellite Network (DTSN) framework as a closed-loop architecture for reliable and intelligent management of LEO satellite constellations. The proposed framework connects the physical satellite network with a synchronized virtual twin and combines real-time telemetry, Integrated Sensing and Communication (ISAC), predictive intelligence, and resilience-oriented control. To validate the concept, we develop a constellation-scale cross-domain co-simulation using the NASA 42 spacecraft simulator and a Python-based DT bridge for a LEO constellation. The DT continuously ingests physical telemetry to manage a multi-domain threat environment, encompassing kinematic drift, hardware failures, and adversarial jamming over a 600-second flight window. By leveraging a predictive lookahead mechanism and an exponential sensor recovery model, the framework successfully isolates compromised nodes and triggers proactive network reconfiguration, thereby ensuring uninterrupted service and dynamic network resilience. These results show the potential of DTSN to support predictive and resilience-oriented satellite network operations.

</details>


### [3] [Pareto-Aware Hierarchical Reinforcement Learning for Online Resource Allocation in RIS-assisted Large-Scale IoT Systems](https://arxiv.org/abs/2608.13032)
*Wenhan Xu,Jiashuo Jiang,Danny H. K. Tsang*

Main category: cs.NI

TL;DR: 本文提出了Pareto-aware autoencoder-assisted RL（PAAERL）框架，通过将高维连续RIS波束成形变量替换为严格代表帕累托最优前沿的低维权重向量，并整合自编码器进行数据驱动的压缩，实现了对RIS辅助物联网网络资源分配的降维优化。


<details>
  <summary>Details</summary>
Motivation: 随着5G和6G网络的发展，可重构智能表面（RIS）成为增强无线通信的关键技术，但优化RIS辅助多用户系统通常会引入高维物理层变量和非凸帕累托最优速率集合，这给实时应用带来严重的计算挑战。传统方法难以处理这些高维问题，导致优化效率低下。

Method: 该方法首先用低维权重向量替代高维连续RIS波束成形变量，这些向量严格表示帕累托最优前沿，从而避免在凸和非凸速率区域之间产生几何信息损失。同时，整合自编码器架构进行二次数据驱动压缩，将优先空间映射到高度压缩的连续潜动作空间，实现了降维与强化学习结合的优化策略。

Result: 通过在多用户移动边缘计算（MEC）网络等实际通信场景中进行广泛模拟，研究表明PAAERL框架显著减少了离线训练时间，加速了在线策略收敛，并大幅降低了整体网络成本，展现出卓越的可扩展性和下一代智能物联网环境中的实际可行性。

Conclusion: PAAERL框架有效解决了RIS辅助多用户系统优化中的高维性和非凸性问题，为5G/6G时代的智能物联网网络提供了一种高效、可扩展且成本低廉的资源分配方案，具有重要的理论价值和实际应用前景。

Abstract: With the rapid evolution of 5G and emerging 6G networks, reconfigurable intelligent surfaces (RIS) have become a critical technology for enhancing wireless communication scenarios. However, optimizing RIS-assisted multi-user systems typically introduces high-dimensional physical layer variables and non-convex Pareto-optimal rate sets, posing severe computational challenges for real-time applications. To address these limitations, this paper proposes a dimension-reduced, hierarchical reinforcement learning (RL) framework, termed Pareto-aware autoencoder-assisted RL (PAAERL), to optimize online resource allocation in RIS-assisted Internet of Things (IoT) networks. Our approach first substitutes high-dimensional continuous RIS beamforming variables with lower-dimensional weight vectors that strictly represent the Pareto-optimal frontier, theoretically avoiding geometric information loss across both convex and non-convex rate regions. To further mitigate the curse of dimensionality in dense networks, an autoencoder architecture is integrated to execute a secondary, data-driven compression phase, mapping the priority space into a highly condensed continuous latent action space. Extensive simulations conducted across practical communication scenarios, including multi-user mobile edge computing (MEC) networks, demonstrate that the proposed PAAERL framework drastically reduces offline training times, accelerates online policy convergence, and significantly decreases overall network costs compared to state-of-the-art benchmarks, underscoring its exceptional scalability and practical viability for next-generation intelligent IoT environments.

</details>


### [4] [Radio-Optical Confluence in Intelligent Edge Networks](https://arxiv.org/abs/2608.13098)
*Akshita Gupta,Devika Dass,Agastya Raj,Carlos Natalino,Marco Ruffini,Paolo Monti,Daniel Kilper*

Main category: cs.NI

TL;DR: 论文讨论了无线接入网络密集化带来的挑战，探讨了从“无线‑‑光融合”到“完全融合”的新架构，旨在提高效率和可扩展性。


<details>
  <summary>Details</summary>
Motivation: 网络密集化带来了性能和部署难题，推动研究者寻找更高效、易扩展的接入架构。

Method: 综述近期进展，分析从传统无线‑‑光收敛技术到“完全融合”架构的演进，强调其设计原理和关键技术。

Result: 全面融合架构通过更紧密集成无线和光层，减少落地空间、提高回传容量和网络弹性，但也引发了设计复杂性、频谱管理和标准化等问题。

Conclusion: 尽管面临挑战，“完全融合”的创新架构有望成为未来密集RAN的主要构建模式，但仍需解决实现中的工程和技术瓶颈。

Abstract: Challenges associated with densification of radio access networks are motivating exploration of more efficient and scalable architectures. We examine recent progress in one direction that involves moving beyond radio and optical convergence to full confluence.

</details>


<div id='cs.MA'></div>

# cs.MA [[Back]](#toc)

### [5] [Entropy-Augmented Multi-Objective Policy Optimization in Multiagent Systems](https://arxiv.org/abs/2608.12534)
*Jamie Santos,Ayhan Alp Aydeniz,Raghav Thakar,Kagan Tumer*

Main category: cs.MA

TL;DR: Adding an entropy bonus to policy fitness encourages behavioral diversity in multi‑objective multi‑agent evolution, yielding up to 48% hypervolume gains over NSGA‑II in rover experiments.


<details>
  <summary>Details</summary>
Motivation: Standard multi‑objective evolutionary algorithms optimize objective‑space diversity but ignore behavior‑space diversity, leading to premature convergence and loss of distinct policies under varying conditions.

Method: Integrate an entropy bonus into the fitness evaluation of each agent, providing a behavior‑space diversity signal while preserving the underlying Pareto optimization framework.

Result: Rover‑domain tests with different reward structures show hypervolume improvements of up to 48% compared with the NSGA‑II baseline.

Conclusion: Behavior‑space diversity via entropy augmentation is a promising, under‑explored avenue for enhancing multi‑objective multi‑agent evolutionary optimization.

Abstract: Autonomous agent teams deployed in settings such as marine and extraterrestrial outposts must coordinate actions to achieve optimal outcomes across multiple competing objectives. Multi-objective evolutionary algorithms such as NSGA-II optimize for diversity in the objective space, but neglect diversity in the behavior space, possibly leading to premature convergence and a collapse in behaviors that may differentiate policies in different external conditions. To address this, we introduce an entropy-augmented policy evaluation strategy that incorporates an entropy bonus into agent fitness scores, discouraging behavioral homogeneity across the evolving population. By augmenting policy evaluation with a behavior-space diversity signal while preserving the underlying Pareto optimization framework, our method is designed to encourage exploration of behaviorally distinct policies in multiagent domains. We evaluate our approach across rover-domain experiments with qualitatively distinct reward structures and observe hypervolume improvements of up to 48% relative to the NSGA-II baseline, suggesting that behavioral diversity is a promising and underexplored direction for improving multi-objective multiagent evolutionary optimization.

</details>


### [6] [Do LLMs Beat Nash? Testing Decentralized Coordination in Self-Play Multi-Agent Games](https://arxiv.org/abs/2608.12547)
*Deborah Sinishaw,Qile Zhu,Edwin Meriaux,Gregory Dudek*

Main category: cs.MA

TL;DR: 在不使用通信的情况下，大型语言模型代理是否仍能协调行动？该研究提出一个一轮、无通信的游戏基准，让13个语言模型在仅被告知对手运行相同模型的情况下进行博弈，并针对底层游戏的纳什均衡进行评估。结果显示，两款 frontier 模型 consistently 超越纳什基准，在若干范式中接近最优联合结果，而大多数开源模型仅获得部分收益，且收益对游戏结构变化敏感。团队 基准 游戏中有四名或更多可交换代理的表现显著下降，特别是在动作空间增大时，这表明驱动二元博弈中自我博弈提升的能力并不转移到更大的多代理团队中。


<details>
  <summary>Details</summary>
Motivation: 研究旨在探索在没有中央控制器和通信的情况下，独立相同模型的实例是否仍能足够合理地推断对手从而超过标准的博弈论无协调游戏基准。

Method: 设计了一个一轮无通信游戏基准，涉及13个语言模型。每个模型仅被告知其对手也运行相同模型，并针对潜在游戏的纳什均衡进行评估。使用两玩家矩阵游戏，涵盖7种原型，每位玩家2至10个动作。

Result: 两款 frontier-hosted 模型 consistent 超越纳什基准，在多个原型中接近最优联合结果。大多数开源模型仅获得部分收益，收益随游戏结构变化显著波动。在四名或更多可交换代理的团队基准游戏中，性能显著下降，特别是在动作空间增大时。

Conclusion: 驱动二元博弈中自我博弈提升的能力并未转移到更大的多代理团队中。

Abstract: Large language model agents deployed without a central controller are often assumed to require communication to coordinate their actions. We ask what remains possible without it: when independent instances of the same model cannot communicate, can they still reason about their counterparts well enough to exceed the standard game-theoretic baseline for uncoordinated play? We introduce a benchmark of one-shot, no-communication games in which each of thirteen language models is told only that its counterparts are running the same model and is evaluated against the Nash equilibrium of the underlying game. In two-player matrix games spanning seven archetypes and two to ten actions per player, two frontier-hosted models consistently exceed their Nash benchmark, approaching the optimal joint outcome in several archetypes, while most open-weight models achieve only partial gains that vary sharply by game structure. Performance degrades substantially in team-based games with four or more interchangeable agents, particularly as the action space grows, suggesting that whatever capability drives self-play gains in dyadic games does not transfer to larger multi-agent teams.

</details>


### [7] [Discovering Efficient and Explainable Communication Topologies for LLM-based Multi-Agent Systems via Causal Inference](https://arxiv.org/abs/2608.12921)
*Junzhi Li,Peng He,Qirui Ji,Wei Wang,Lixiang Liu,Chuxiong Sun*

Main category: cs.MA

TL;DR: E2-Explainer通过因果归因方法解释LLM多智能体系统的通信拓扑，识别关键子图并可用于压缩通信成本。


<details>
  <summary>Details</summary>
Motivation: 现有拓扑生成仅靠任务奖励，缺乏对通信边选择原因的解释，难以定位关键子图。

Method: 将拓扑解释建模为因果归因问题，使用Granger式目标评估每条边的任务影响，得到预算化子图，再通过摊销解释器实现高效后置解释。

Result: 在多项推理与编码基准上，E2-Explainer成功识别保留协作效果的关键子图，并可直接执行以削减冗余通信，保持竞争性能。

Conclusion: E2-Explainer提供可解释的通信拓扑分析，既揭示协作机制，又实现通信成本显著降低。

Abstract: The performance of large language model (LLM)-based multi-agent systems (MAS) largely depends on effective communication topologies. Existing topology generation methods, however, typically learn communication topologies through black-box optimization driven solely by task-level rewards. While effective, such optimization provides little insight into why particular communication edges are selected, making it difficult to identify the critical communication subgraphs responsible for successful collaboration. To address this limitation, we propose E2-Explainer, a model-agnostic framework for providing interpretable explanations of communication topologies produced by arbitrary topology generators. Specifically, we formulate topology explanation as a causal attribution problem that identifies compact communication subgraphs supported by edge-level evidence of task preservation. We obtain this evidence with a Granger-style objective that measures how masking each communication channel changes the task outcome and the stability of the final response. The resulting budgeted subgraphs are then distilled into an amortized explainer, enabling efficient post-hoc explanation without repeated edge-level evaluations at deployment. Extensive experiments on multiple reasoning and coding benchmarks demonstrate that E2-Explainer identifies critical communication subgraphs that preserve successful collaboration. These subgraphs can also be executed directly to prune redundant communication edges, substantially reducing communication costs while maintaining competitive task performance.

</details>


### [8] [Reconcile Once, Write Anytime: A Trust-Tiered Librarian and a Multi-Agent Writer for Drift-Free, Point-in-Time Research](https://arxiv.org/abs/2608.12984)
*Xing Zhang,Yanwei Cui,Guanghui Wang,Peiyang He*

Main category: cs.MA

TL;DR: 该论文提出了一个两层Agent系统，旨在解决大语言模型长文报告中存在的知识漂移、自相矛盾和缺乏来源追踪等问题。通过一个确定性的"图书管理员"将带时间戳的来源存储在一个分层可信本体中（包括证据卡片、权威指标账本和声明图），确保知识的实时性和可追溯性。然后，一个可移植的多Agent"作家"运行时可以在任意知识截止点T生成无矛盾、基于证据的报告，仅读取符合时间条件的证据，并接收来自红队评审的反馈以更新图书管理员。实验证明，该系统有效地消除了交叉断层面的矛盾（6,845处），在22个金标准测试中均正确（而基础模型仅正确9个），避免了媒体来源数据的泄露和政府统计数据的篡改，红队反馈机制能够自我纠正，并在多次重放中确保零超前违规，同时通过困难分层模型路由实现了超过全Opus模型的质量水平和3.7倍的加速。


<details>
  <summary>Details</summary>
Motivation: 大语言模型生成的长文报告存在知识不一致、自相矛盾和来源追踪不足的问题，例如相同指标显示不同数值，谣言被视为权威来源。现有方法（如基于查询的RAG）依赖于实时检索和未经验证的知识，无法保证报告的质量和可追溯性。

Method: 构建了一个两层Agent系统：1) 确定性"图书管理员"将带时间戳的来源以信任等级存储在一个本体中，包含证据卡片、权威指标账本和声明图，形成一个始终更新的知识库；2) 可移植的多Agent"作家"运行时根据指定的知识截止点T，仅读取符合时间条件的证据，生成无矛盾、基于证据的报告，并接收来自红队的反馈以更新图书管理员。此外，采用了困难分层模型路由技术，动态选择合适的模型。

Result: - 共享指标账本消除了6,845个交叉断层面上的矛盾。- 信任优先选择在22个金标准测试中全部正确，而基础模型仅正确9个。- 信任分层确保零媒体来源数据泄露和政府统计数据取代公司自身报告的情况。- 红队反馈机制能够自我纠正，在重放时实现零超前违规。- 在7个知识截止点上，系统从235,373个证据卡片扩展到555,312个，但始终保持零超前违规。- 困难分层模型路由在保持高质量的同时，比串行全Opus模型运行速度提高了3.7倍。

Conclusion: 两层Agent系统有效地解决了大语言模型长文报告中的知识一致性和来源追踪问题。它提供了一个确定性、可追溯的知识库，并在报告生成过程中实现了高质量、无矛盾和实时更新。实验证明了系统的有效性和可靠性，该系统有望显著提升AI生成长文报告的质量和可信度。

Abstract: Long-form research reports generated by large language models drift, contradict themselves, and lose provenance: the same metric appears with different values, and rumor is quoted as confidently as an audited filing. We present a two-tier agentic system that separates a maintained, point-in-time knowledge library from report writing. A deterministic "librarian" ingests timestamped sources into a trust-tiered ontology, layering evidence cards, an authoritative metric ledger, and a claim graph into an always-current source of truth, not per-query RAG over raw chunks. A portable multi-agent "writer" runtime then composes a contradiction-free, evidence-grounded report at any knowledge cutoff T, reading only evidence with as_of <= T (no look-ahead); red-team verdicts flow back into the librarian. We evaluate on a self-collected, public corpus of 6,130 sources yielding 555,926 evidence cards (SEC EDGAR filings across 295 issuers and 11 sectors, U.S. Bureau of Labor Statistics releases, and Wikipedia). From the one library we compose four point-in-time reports on distinct theses and run eight reproducible experiments, whose headline metrics come from a deterministic quality-control gate, itself validated by defect-injection meta-evaluation at recall 1.0 and precision 1.0. A shared metric ledger removes 6,845 cross-section contradictions to zero. Tier-first selection is correct on 22/22 gold cases where a popularity-first baseline scores only 9/22; trust tiering leaks zero media-sourced numbers, and no government statistic displaces a company's own filing. A red-team refutation propagates back and self-corrects a later run with zero manual edits. Replay exhibits zero look-ahead violations across seven cutoffs while the library grows from 235,373 to 555,312 cards. Difficulty-tiered model routing exceeds the all-Opus quality ceiling while running 3.7x faster than serial.

</details>


<div id='cs.SE'></div>

# cs.SE [[Back]](#toc)

### [9] [SynWeaver: Website-Prior Task and Trajectory Co-Synthesis for Web Agents](https://arxiv.org/abs/2608.12429)
*Ruitao Wang,Yuwen Hao,Menglin Yang*

Main category: cs.SE

TL;DR: SynWeaver 通过结构化网站探索构建覆盖广泛功能的网站地图，利用地图得到页面级和转移级监督训练 UI‑aware 模型，再进行任务与轨迹的协同合成并验证修复，从而在未见网站上实现更好泛化。


<details>
  <summary>Details</summary>
Motivation: 现有基于探索的数据合成方法难以覆盖网站全部功能且容易产生虚假任务，导致合成轨迹多样性和效率受限，且缺乏网站先验知识使得代理在未见网站上的泛化能力弱。

Method: 首先对目标网站进行结构化探索，得到包含多种不同页面状态和可执行交互的网站地图；从地图提取页面级和转移级监督信息，训练具备网站先验的 UI‑aware 模型以产生更可靠的任务建议；接着采用协同任务‑轨迹合成框架，当任务与执行轨迹不一致时 jointly 更新，随后进行验证与修复，得到可执行且语义对齐的监督数据。

Result: 在 WebArena 和 WebVoyager 基准上，SynWeaver 显著优于现有强基线，无论是在域内还是跨域泛化任务上都提供了更有效的监督信息，轨迹质量和成功率均有提升。

Conclusion: 通过网站先验引导的任务‑轨迹协同合成，SynWeaver 成功缓解了功能覆盖不全和虚假任务问题，为网站代理的泛化提供了更强的监督基础。

Abstract: Web agents often struggle to generalize to unseen websites because they lack website-specific supervision. Recent exploration-based data synthesis methods reduce manual annotation, but they still face two key limitations: they often fail to cover the full functionality of a website, and without sufficient website prior knowledge, they tend to propose hallucinated tasks, which in turn limits the diversity and efficiency of downstream trajectory synthesis. We present \textbf{SynWeaver}, a website-prior task-trajectory co-synthesis framework designed to address these challenges. SynWeaver first performs structured website exploration and constructs a website map that covers a broad set of functionally distinct page states and executable interactions on the target website. It then derives page-level and transition-level supervision from this map to train a UI-aware model with website-specific priors, enabling more grounded task proposals. Finally, SynWeaver performs collaborative task-trajectory synthesis, jointly updating the task and execution trajectory when they become inconsistent, and then verifies and repairs the collected results to produce executable, semantically aligned supervision. Experiments on WebArena and WebVoyager demonstrate that SynWeaver consistently outperforms strong synthesis baselines and yields more effective supervision for both in-domain and out-of-domain generalization.

</details>


### [10] [Specification-first convergence with an AI coding agent: a case study of dismantling a core architectural invariant across 189 files in a 717k-line codebase with no test oracle and no human code review](https://arxiv.org/abs/2608.12440)
*Joel Abenhaim*

Main category: cs.SE

TL;DR: AI代理通过规格优先协议，在未有人工审查的情况下，成功完成了大型TypeScript代码库的核心不变量重构，修正了201个缺陷，行为符合规范。


<details>
  <summary>Details</summary>
Motivation: 传统增量重构被认为几乎不可行，需要完全重写，因而探索AI代理在无人工干预下进行大规模架构重构的可行性。

Method: 代理先形式化制定目标规范，经过14轮源码审计确保规范匹配，然后以原子方式实现，利用编译/测试反馈循环，随后进行17轮代码审计以验证与冻结规范的一致性，直至连续两次审计零缺陷为止。

Result: 共修正201个缺陷，涉及189个文件（新增31个），两次提交共288个文件，插入34,770行，删除16,422行；历时三天，成本2,430美元；后续约三十次会话中软件始终按规范运行，未观察到错误。

Conclusion: 在严格的规格优先协议下，AI代理能够在无人工审查和无预先 oracle 的情况下完成大规模架构重构，证明该方法对于被认为不可增量重构的复杂变更也是可行的。

Abstract: This paper reports a single, fully instrumented case study of a large-scale architectural refactoring by an AI coding agent under a specification-first protocol, with no human review of the generated code and no pre-existing oracle to validate the target behaviour. The task, dismantling a central invariant across a large interdependent codebase, was assessed by the author as effectively infeasible through incremental refactoring, the kind of change that conventionally calls for a rewrite instead. Under the protocol described here, the agent completed it successfully.
  The system is a 717,725-line production TypeScript application across 3,648 files. The task required dismantling a core lifetime invariant: the guarantee that a UI panel remains open for the duration of an AI request. The target behaviour was that a streaming generation survives the closing of its panel and can be reattached, on reopening, to the same live stream with no loss or duplication.
  The protocol: formal specification by the agent, 14 refinement cycles auditing that specification against the source code, atomic implementation, a compile/test feedback loop, then 17 verification cycles auditing the code against the frozen specification. Across 31 audit passes, 201 defects were corrected before any human executed the program. The convergence criterion was empirical: two consecutive verification passes returning zero findings.
  The change touched 189 files (31 new); with the extraction phase, the two commits total 288 files, 34,770 insertions, 16,422 deletions. Across the first and roughly thirty later sessions, the software behaved as specified, no bug observed. Elapsed: three days; cost: USD 2,430.
  The full specification and raw session logs, 1,500+ pages in French, are published as evidence, allowing inspection of the process and submission to a language model for consistency checking.

</details>


### [11] [Does It Render Everywhere? A Study of Cross-Environment Compatibility in MLLM-Generated Webpages](https://arxiv.org/abs/2608.12518)
*Ziyun Guo,Jingyu Xiao,Yuqiang Sun,Yintong Huo*

Main category: cs.SE

TL;DR: Websites made by AI tools often look broken when viewed on different devices or browsers due to compatibility issues. A new test tool, XCompat, is better at finding these problems than older methods.


<details>
  <summary>Details</summary>
Motivation: Existing checks for AI-generated webpages only test on one fixed setup, not real-world variations. 68% had issues like shrinking pages or distorted images.

Method: Tested 2,032 AI-generated webpages across 8 tools and 9 device-browser combos (e.g., phones vs. laptops). Find bugs by comparing screenshots and code structure.

Result: XCompat detector found 90.3% of compatibility issues in tests. 41% had major layout breaks, 27% had partial element failures.

Conclusion: Most AI-generated webcode lacks real-world compatibility. Researchers release tools/data to improve reliability.

Abstract: Multimodal Large Language Models (MLLMs) have been increasingly adopted to automate webpage generation from visual designs (e.g., screenshots). However, existing evaluations are limited to visual fidelity assessment under a fixed browser-device configuration. Such a setting overlooks the cross-environment rendering compatibility for real-world deployments.
  To address this gap, we present the first systematic empirical study of cross-environment compatibility in AI-generated webpages. Specifically, we construct WebCompat, a dataset of 2,032 annotated instances, comprising webpages generated by 8 representative AI tools, each rendered across 9 browser-and-device combinations. We analyze the prevalence of compatibility issues, their user-perceptible symptoms, and underlying code-level root causes. Our findings reveal that 68% of generated webpages exhibit at least one compatibility issue, underscoring the pervasive reliability concerns surrounding MLLM-generated front-end artifacts. The most prevalent symptoms are failures that disrupt the entire page layout (88.3%): pages shrink directly to fit the target screen with too small fonts, or exhibit scale mismatches that produce cut-off content. Failures localized to individual elements, such as image distortion or missing components, are comparatively less common (13.4%). Furthermore, although most MLLMs incorporate responsive design patterns into the generation, they fail to properly implement these codes.
  Guided by the findings, we develop XCompat, a lightweight offline compatibility issue detector that combines visual screenshots and the structural DOM tree for analysis. It achieves an F1 score of 0.903 on the WebCompat-test, outperforming the existing compatibility checking tools and LLM baselines. All datasets and tools are released to support future research on rendering reliability in MLLM-based front-end code generation.

</details>


### [12] [Memorization Diagnostics for Code LLMs Should be Scale-Aware](https://arxiv.org/abs/2608.12771)
*Prateek Kumar Rajput,Abdoul Aziz Bonkoungou,Alberick Euraste Djiré,Xunzhu Tang,Yewei Song,Iyiola Emmanuel Olatunji,El Hacen Diallo,Jacques Klein,Tegawendé F. Bissyandé*

Main category: cs.SE

TL;DR: 大型语言模型在代码任务上是记忆还是真正理解仍有争议，现有评估方法在大规模模型上失效，需分离表示负荷与记忆。


<details>
  <summary>Details</summary>
Motivation: 现有探针方法在大模型上无法检测记忆，且小模型受干扰大模型不受影响，需明确表示负荷与记忆两者的区别。

Method: 通过可逆数学变换对数值问题进行处理，将表示负荷与记忆分离，评估模型在不同表象负荷下的表现。

Result: 大规模编码模型能够吸收大量表示负荷并仍能收敛到正确的解决方案家族，说明其适应不同表象的能力强。

Conclusion: 未来的评估应分离记忆与表示负荷，而非使用混合方法，以更准确衡量模型的可用性和泛化性。

Abstract: The extent to which large language models for code rely on memorization over genuine understanding remains highly debated. While current literature frequently reports widespread memorization, evaluating the underlying probing techniques across dense architectures reveals a severe breakdown in their utility at scale. Traditional encoder-style probes using perturbations such as synonym fuzzing or dead-code insertion struggle to expose memorization in scaled models, even on known-contaminated benchmarks, and decoder-style probes that rely on log probabilities show similar performance degradation. The specific mode of failure for these probes, particularly why such techniques disrupt smaller models but fail to impact larger ones, motivates us to untangle representation load from memorization rather than treating them as a single phenomenon. By applying invertible mathematical transforms to numeric problems, we isolate these two factors and reveal that scaled encoders successfully absorb substantial representation load while still converging on the correct family of solutions. In practical software engineering, this ability to adapt to varying surface forms is what truly matters for usability and generalizability in LLM and agentic applications. Whether a specific solution was seen during training becomes a much less pressing question because although memorization inflates scores on contaminated benchmarks, factoring out representation load makes it debatable how much we should truly care if a functional answer was originally memorized. Future evaluations must therefore be built around separating these phenomena rather than relying on methodologies that quietly entangle them.

</details>


### [13] [Dissecting Software Graphs: Structural Insights for Driver-Guided Fuzzing](https://arxiv.org/abs/2608.12859)
*Baihong Chen,Hua Ming,Weifeng Pan,Tian Xie,Haipeng Cai,Wen Li*

Main category: cs.SE

TL;DR: 研究提出了一种基于静态调用图的多驱动模糊测试框架，能动态投影不同驱动程序的覆盖率，构建驱动诱导子图。实证分析了 27 个 OSS‑Fuzz C/C++ 项目（43 个可执行文件，854 种驱动配置），结果表明多驱动模糊测试比最佳单驱动 baseline 表现更优，节点覆盖率提高 27.9% – 边缘覆盖率提高 73.5%，并发现了 11 个被单驱动模糊测试错过的唯一 bug。然而，不同驱动程序的贡献不均衡，子图在凝聚力、碎片化、模块化、覆盖重叠等方面存在显著差异，而残留未探索区域遵循周期性模式而非均匀下降趋势。这表明多驱动模糊测试本质上是一个结构探索问题。


<details>
  <summary>Details</summary>
Motivation: 现有模糊测试评估侧重于覆盖率和 bug 数量，忽略了不同执行模式（由命令行选项/子命令/配置标志驱动）如何划分、重叠和漏掉软件结构，以及这些差异如何影响模糊测试的有效性。

Method: 提出基于静态调用图的结构抽象，将驱动特定的动态覆盖投影到共同骨干上以提取驱动诱导的子图。制定四阶段方法：(1) 骨干构建，(2) 模糊测试和 profiling，(3) 基于图的分析，(4) 以研究问题为导向的评估。并将其应用于 27 个 OSS-Fuzz C/C++ 项目，涵盖 43 个可执行文件和 854 种驱动配置。

Result: 多驱动模糊测试在相同的预算内表现更优：调用图节点覆盖率提高 27.9% – 控制流图边缘覆盖率提高 73.5%，并发现了 11 个被单驱动模糊测试错过的唯一 bug。尽管如此，不同驱动程序的贡献不均衡，子图在凝聚力、碎片化、模块化、覆盖重叠等方面差异显著，而残留未探索区域遵循周期性模式而非均匀下降趋势。

Conclusion: 多驱动模糊测试的性能差异源于结构探索的差异；基于图的方法揭示了驱动程序之间的不均衡性和模式，这对于优化模糊测试策略和理解多模式软件的探索行为至关重要。

Abstract: Many software systems expose multiple execution modes through command-line options, subcommands, and configuration flags. For such programs, fuzzing depends on both mutated inputs and the invoked mode. Yet evaluations still focus on coverage and bug counts, leaving unclear how execution modes partition, overlap, and miss software structure, and how these differences affect effectiveness. We present an empirical study of software structure under multi-driver fuzzing. We propose a structural abstraction that uses a static call graph as a shared backbone and projects driver-specific dynamic coverage onto it to derive driver-induced subgraphs. Based on this abstraction, we develop a four-phase methodology for backbone construction, fuzzing and profiling, graph-based analysis, and research-question-driven evaluation. We apply it to 27 OSS-Fuzz-derived C/C++ projects, spanning 43 executables and 854 driver configurations. Under the same total budget, multi-driver fuzzing outperforms the best single-driver baseline, increasing covered call-graph nodes by 27.9% and CFG-edge coverage by 73.5%, and revealing 11 unique bugs and abnormal behaviors largely missed by single-driver fuzzing. However, driver contributions are uneven, subgraphs differ substantially in cohesion, fragmentation, modularity, overlap, and residual under-exploration follows recurring regimes rather than a homogeneous tail. These results show that multi-driver fuzzing is fundamentally a structural exploration problem.

</details>


### [14] [Requirements-Augmented Generation for Trustworthy Acceptance Testing of LLM-Based Software](https://arxiv.org/abs/2608.12970)
*Fanyu Wang,Chetan Arora,Zhenping Xie,Yonghui Liu,Kla Tantithamthavorn,Aldeida Aleti,Siwei Jiang*

Main category: cs.SE

TL;DR: 本文提出了一种针对基于大语言模型的软件（LBS）的自动化验收测试框架，旨在解决由于LBS的随机性和上下文依赖性导致的传统测试预言（test oracles）失效问题。


<details>
  <summary>Details</summary>
Motivation: 传统的确定性验收测试无法应对LBS的随机性行为，因为相同的查询在不同用户画像或上下文中需要不同的响应，因此需要能够自主解释用户指令并推断意图的自动化框架。

Method: 提出了两项技术贡献：一是需求增强生成（REAG），通过自适应RAG和自我推理来生成上下文相关的测试预言；二是置信度校准的级联判定（confidence-calibrated cascade judgment），利用模拟专家一致性对判定结果进行量化，通过符合性风险控制（conformal risk control）实现可靠的决策过滤。

Result: 工业案例研究显示，REAG的预言质量评分为3.91/5，82%的案例达到合格或边缘水平；级联判定准确率达98.8%，将预言质量提升至4.30，并比单一判别基准提高了31.7%的成本效率。

Conclusion: 该框架有效解决了LBS验收测试中的预言生成与可靠性问题，并在工业生产环境中验证了其可行性。

Abstract: LLM-based software (LBS) integrates large language models as core components to deliver flexible, personalised responses. Unlike traditional software with deterministic outputs, LBSs exhibit context-dependent, stochastic behaviour that renders classical acceptance testing and test oracles insufficient: the same query may require fundamentally different responses depending on user personas and software context. This gap creates an urgent need for automated acceptance testing frameworks that autonomously interpret user instructions, while reliably inferring user intentions in a changing environment. In this paper, we present an automated acceptance testing framework for LBS with calibrated verdict reliability via two technical contributions. First, we introduce Requirements-Augmented Generation (REAG), which interprets user intentions by retrieving relevant software requirements, domain knowledge, and personas via adaptive RAG and self-reasoning to generate context-aware test oracles. Second, recognising that oracle generation may retrieve irrelevant constraints, misinterpret intent, or hallucinate requirements, we introduce a confidence-calibrated cascade judgment. This method quantifies verdict reliability via simulated expert agreement -- accepting high-confidence verdicts, escalating ambiguous cases, or abstaining when uncertain -- with empirical reliability guarantees backed by conformal risk control. An industrial case study on a production nutrition advisory application demonstrates that REAG achieves a 3.91/5 oracle quality score, reaching qualified or marginal oracle quality in 82% of cases. The confidence-calibrated cascade achieves 98.8% accuracy, improves oracle quality from 3.91 to 4.30 by filtering unqualified outputs, and delivers a 31.7% cost-efficiency improvement over single-judge baselines, validating industrial viability

</details>


### [15] [How Powerful are LLMs in Generating Formal Program Specifications?](https://arxiv.org/abs/2608.13077)
*Fanpeng Yang,Xing Li,Shuling Wang,Jie An,Zeyu Sun,Shenghua Feng,Wenhan Wang,Weiyi Wang,Naijun Zhan,Fanjiang Xu*

Main category: cs.SE

TL;DR: Coins是一个基于Rocq的评估框架，用于通过实例化规范并生成证明义务来评估软件规范质量。研究发现，LLM生成规范的能力仍存在挑战，且验证复杂度可能掩盖规范质量的真实差异。


<details>
  <summary>Details</summary>
Motivation: 形式化验证虽然能提供强大的软件正确性保证，但其高成本限制了广泛应用。现有方法需要验证实现一致性或证明语义等价，这两者都很困难且可能混淆证明难度与规范质量。需要更有效的方法来评估LLM生成的规范质量。

Method: 引入Coins框架，一个基于Rocq的评估框架，通过在可信测试用例上实例化规范并生成具体证明义务来评估规范质量。这种设计符合形式推理的非对称特性：成功的证明提供可靠证据，而失败的证明则具有内在模糊性。

Result: 大规模研究HumanEval数据集，使用人工编写的Rocq规范。结果表明规范生成仍然是艰巨的挑战，验证复杂度可以掩盖规范质量的真实差异。准确的规范评估比单纯依赖模型规模更为关键，基于测试用例的形式推理提供了更忠实和区分性的进展衡量标准。

Conclusion: 准确的规范评估比模型规模本身更为核心。Coins框架展示了测试驱动的形式推理对于理解LLM在规范合成中的能力至关重要，为未来的形式化验证与LLM协同工作提供了重要指导。

Abstract: Formal verification provides strong guarantees of software correctness, but its adoption is limited by the high cost of writing precise formal specifications. While recent large language models (LLMs) have shown strong capabilities in theorem proving and verified code generation, their true ability to generate program specifications remains unclear. Existing evaluations require either verifying implementation conformance or proving semantic equivalence between specifications, both of which are formidably difficult and may conflate proof difficulty with specification quality. To address this problem, we introduce Coins, a Rocq based evaluation framework that assesses specification quality by instantiating specifications under evaluation on trusted test cases and generating concrete proof obligations. This design aligns with the asymmetric nature of formal reasoning, where successful proofs provide reliable evidence while proof failures are inherently ambiguous. Using Coins, we conduct a large scale study on HumanEval with a curated set of human written Rocq specifications. Our results show that specification generation remains a formidable challenge, and that verification complexity can obscure genuine differences in specification quality. Overall, we find that accurate specification evaluation, rather than model scaling alone, is central to understanding the power of LLMs for specification synthesis, and that test case based formal reasoning offers a more faithful and discriminative measure of progress.

</details>


### [16] [Can Formal Specifications Be Synthesized from Tests Alone?](https://arxiv.org/abs/2608.13240)
*Tianhai Liu,Maximilian Müller,Tobias Hey,Vitus Lüntzel,Muhammad Minhas,Anne Koziolek,Bernhard Beckert*

Main category: cs.SE

TL;DR: 本研究提出一种新方法，利用LLM从测试代码和动态执行轨迹中推断形式化规范，避免了白盒访问带来的知识产权风险和部署成本。


<details>
  <summary>Details</summary>
Motivation: 传统形式化规范虽然提供强保证，但手动编写成本高昂。现有的LLM自动推断方法依赖白盒访问，这在工业应用中面临知识产权风险和部署成本等障碍。

Method: 该方法仅使用测试代码和动态执行轨迹来推断候选规范，LLM只能观察程序接口、输入和输出或状态变化，而实现内部保持隐藏。候选规范通过有界模型检查进行本地验证，反馈指导迭代优化。

Result: 在SpecGenBench基准上，测试能够引导LLM生成有意义的Java Modeling Language（JML）规范，但也暴露了检查器兼容性和诊断反馈作为可靠优化关键挑战。

Conclusion: 该工作展示了一种安全且高效的LLM驱动的形式化规范推断方法，在保持隐私保护的同时有效降低了开发成本。

Abstract: Formal specifications offer strong guarantees, but remain costly to write manually. Recent LLM-based approaches automate this by inferring specifications from source code, yet their reliance on white-box access poses barriers to industrial adoption due to intellectual property risks and deployment costs. Our approach uses LLMs to infer candidate specifications solely from test code and dynamic execution traces: the LLM observes only the program interface, selected inputs, and corresponding outputs or state changes, while the implementation internals remain hidden. Candidate specifications are validated locally using bounded model checking, with feedback guiding iterative refinement. Initial results on the SpecGenBench benchmark suggest that tests can guide LLMs towards meaningful Java Modeling Language specifications, while also highlighting checker compatibility and diagnostic feedback as key challenges for reliable refinement.

</details>


### [17] [Refine After Generation: Toward Correct and Concise Patches in LLM-based Program Repair](https://arxiv.org/abs/2608.13292)
*Wenqiang Luo,Jacky Keung,Xiaoyu Shi,Yicheng Sun,Boyang Yang,Zhou Yang,Haoye Tian*

Main category: cs.SE

TL;DR: 大型语言模型（LLM）驱动的自动程序修复（APR）已能常规解决真实仓库级别的问题，但生成的补丁在测试通过之外几未被审查。本文识别出补丁冗余为被忽视的主要问题，在SWE-bench Verified上分析28种 state-of-the-art 方法后发现，即使是成功的补丁也比开发者补丁更大更复杂，中位数方法产生的总变更多出 121.78%，净变更多出 80.91%，循环复杂度高出 43.99%。这些冗余根植于迭代细化和广泛上下文等能力导向设计选择，而非输出格式或最小化提示词等表面控制。作者提出 RECAP，一种在生成后进行补丁细化的轻量级插件，通过监督微调和直接偏好优化使用蒸馏推理痕迹训练，在多源补丁对上实现了体积与正确性的更佳权衡。


<details>
  <summary>Details</summary>
Motivation: 尽管 APR 能力突破，但补丁体积和复杂度的膨胀严重影响代码审查的可读性与可维护性。现有研究多关注修复成功与否，却忽视了补丁的体量与开发者补丁的差异。本文动机在于揭示冗余成因，并探索在不牺牲修复能力的前提下压缩补丁的可行路径。

Method: 提出 RECAP，一种可插拔的后生成补丁细化适配器。其细化器在构建的补丁对数据集上通过监督微调（SFT）和直接偏好优化（DPO）进行训练，数据来源于多个自动修复系统的补丁对。RECAP 在生成后介入，利用蒸馏的推理痕迹引导补径从冗余中解脱，在保持或提升修复成功率的同时显著降低总变更数与净变更数。

Result: 在四个宿主系统上，现有的提示优化、commit-untangling 与最小化意识基线仅在牺牲 49 至 217 个已修复实例的情况下才能压缩补丁。相比之下，RECAP 将平均总变更从 +242.14% 降至 +4.24%，净变更从 +348.24% 降至 -39.75%，并最多保留或提升 42 个解决实例，实现了比现有基线更优的体量-正确性权衡。

Conclusion: 研究表明，补丁的最小化不能仅仅等同于语法压缩，且将最小化从生成过程中解耦，是通往更易审查、更易维护修复的实际路径。

Abstract: Large language models (LLMs) have advanced automatic program repair (APR) to the point where agentic systems routinely resolve real-world, repository-level issues. Yet the generated patch has received little scrutiny beyond whether it passes tests.
  In this paper, we identify patch verbosity as a major yet overlooked concern in LLM-based APR. Characterizing 28 state-of-the-art approaches on SWE-bench Verified, we find that even successful patches are consistently larger and more complex than developer patches, with the median approach producing 121.78% more total changes, 80.91% more net changes, and 43.99% higher cyclomatic complexity. We further show that this verbosity is rooted in capability-oriented design choices such as iterative refinement and broad context, and can hardly be reduced by surface-level controls such as output format or minimality prompts. Motivated by these findings, we formulate post-generation patch refinement and propose RECAP, a lightweight, plug-and-play adapter that attaches to existing repair frameworks after generation. RECAP's refiner is trained via supervised fine-tuning and direct preference optimization with distilled reasoning traces, on a dataset of patch pairs we construct from multiple sources. Across four host systems, prompting, commit-untangling, and minimality-aware baselines reduce patch size only by sacrificing 49 to 217 resolved instances. In contrast, RECAP achieves a substantially better size-correctness tradeoff, cutting average total changes from +242.14% to +4.24% and net changes from +348.24% to -39.75% relative to developer patches while preserving or improving resolution by up to 42 instances. Our results indicate that minimality cannot be simply reduced to syntactic compression, and that decoupling minimization from generation offers a practical path to more reviewable repairs.

</details>


### [18] [Integration-First Structural Coverage for Embedded Software:Trace-Based Evidence, Hybrid Runtime Analysis, and Cross-Variant Consolidation](https://arxiv.org/abs/2608.13322)
*Alexander Weiss,Albert Schulz,Michael Wittner*

Main category: cs.SE

TL;DR: 该研究提出了一种整合式覆盖率策略，结合过程层面的集成优先覆盖率方法和技术层面的嵌入式追踪与混合运行时分析（hRA），解决了传统结构化覆盖率在嵌入式项目中仅在单元级别收集而无法反映真实测试完整性的问题。通过将集成和系统测试作为基线测量，并建立显式的闭环机制来填补残差缺口，确保覆盖率被确认为已覆盖或有充分理由，而非仅仅是“已覆盖”。同时引入嵌入式追踪作为观察路径，利用最小且保持语义的可观测性支架，在优化后的二进制中保持决策和条件边界的可区分性，使分支、条件和MC/DC测量在发布级二进制上变得可行。Hyper Coverage层进一步整合跨测试层次、测试运行、变体和构建配置中的证据，并暴露每个相关变体中未被测试的源行。


<details>
  <summary>Details</summary>
Motivation: 在嵌入式项目中，结构化覆盖率通常仅在单元级别收集，因为那里仪器化和可观测性成本较低。这导致了一个不匹配：最具代表性的完整性信号来自于在设备上执行的集成和系统测试，但传统的仪器化会扰乱时间、内存占用和并发行为；而纯粹基于追踪重建的覆盖率一旦编译器进行激进优化，就会失去可靠性。因此，需要一种能够弥合这些差距的方法，既能在过程层面确保测试完整性，又能在技术层面提供可靠的覆盖率证据。

Method: 从过程端，他们描述了一种集成优先的覆盖率策略，将集成和系统测试作为基线测量，通过显式的闭环机制驱动剩余缺口，确保覆盖率被确认为已覆盖或有充分理由，而非仅仅是“已覆盖”。从技术端，他们使用嵌入式追踪作为观察路径，并引入混合运行时分析（hRA）：一个最小且保持语义的可观测性支架，在优化后的(-O3)构建的追踪流中保持决策和条件边界的可区分性，同时所有覆盖状态和计数仍然是离目标的。这种方法将对象到源映射从启发式重构转变为可审查的证据，使分支、条件和MC/DC测量在发布级二进制上变得可行。此外，他们还描述了Hyper Coverage，一层整合框架，合并跨测试层次、测试运行、变体和构建配置中的证据，并在每个相关变体中暴露未被测试的源行。

Result: 该方法成功解决了嵌入式项目中结构化覆盖率仅在单元级别收集的问题，通过集成优先策略和Hybrid Runtime Analysis（hRA）实现了对真实测试完整性的准确评估。同时，Hyper Coverage层实现了跨测试层次、测试运行、变体和构建配置的证据整合，提供了更全面和可靠的覆盖率视图，支持在发布级环境中进行分支、条件和MC/DC等关键质量度量的准确判断。

Conclusion: 该研究提出了一种全新的覆盖率评估方法，结合过程层面的集成优先策略和技术层面的嵌入式追踪与混合运行时分析，有效解决了嵌入式项目中覆盖率评估的不一致性问题。通过将集成和系统测试作为基线，并建立显式的闭环机制来验证覆盖率的有效性，同时利用最小且保持语义的可观测性支架在优化后的二进制中保持决策和条件边界的可区分性，最终实现了从单纯的“已覆盖”向“已覆盖或有充分理由”的严谨覆盖率确认，为嵌入式软件的质量保证提供了新的理论基础和实践工具。

Abstract: Structural coverage is widely used as evidence that testing is complete, yet in embedded projects it is predominantly collected at unit level, simply because that is where instrumentation and observability are inexpensive. This produces a mismatch. The most representative completeness signal would come from integration and system tests executed on the device under test, but classical instrumentation perturbs timing, memory footprint and concurrency behaviour, while purely trace-reconstructed coverage loses reliability for decisions and conditions as soon as the compiler optimizes aggressively. We address this mismatch from both ends. On the process side we describe an integrationfirst coverage strategy that treats integration and system tests as the baseline measurement and drives the residual gaps through an explicit closure loop, so that completeness is established as covered or justified rather than as covered alone. On the technical side we use embedded trace as the observation path and add hybrid runtime analysis (hRA): a minimal, semantics-preserving observability scaffolding that keeps decision and condition boundaries distinguishable in the trace stream of an optimized (-O3) build, while all coverage state and counting remain off-target. This converts object-to-source mapping from a heuristic reconstruction into reviewable evidence and makes branch, condition and MC/DC measurement practical on release-like binaries. Finally we describe Hyper Coverage, a consolidation layer that merges evidence across test levels, test runs, variants and build configurations, and that exposes source lines which remain untested in every relevant variant.

</details>


### [19] [Does Fixing Break Security? An Empirical Study of Security Degradation in Iterative LLM-Driven Infrastructure-as-Code Repair](https://arxiv.org/abs/2608.13404)
*Benjamin Agyekum,Fabio Santos*

Main category: cs.SE

TL;DR: Iterative LLM repair for IaC introduces security regressions in ~3.3% of scenarios under strict criteria, driven mainly by resource restructuring. Most regressions self-correct within 1-2 iterations.


<details>
  <summary>Details</summary>
Motivation: Security regression is a critical but underexplored risk in iterative IaC repair cycles, where prior work only reported cumulative security metrics without analyzing per-iteration degradation potential.

Method: Analyzed 5,968 scenario timelines across 15 configurations using Checkov validation, tracking code changes and benchmark metrics through 5 repair iterations with both inclusive and exclusive failure detection approaches.

Result: Strict detection showed 3.3% regression scenarios (5.2% transitions) vs. 13.8% under inclusive detection, with 79% of regressions explained by resource restructuring. Regressions were associated with higher code churn (2.6x) and volatility (4.9x), yet 36.6% self-corrected within 1.2 iterations average.

Conclusion: While iterative IaC repair does introduce measurable security regressions, the conservative failure rate is manageable (3.3%) with strategic iteration budgeting. Recommendation: Use strict validation detection thresholds and limit repair cycles to 3 iterations maximum for security-critical workflows.

Abstract: Background: Iterative feedback loops are the dominant paradigm for improving LLM-generated Infrastructure-as-Code (IaC): validators such as Checkov and terraform validate feed error signals back for successive repair attempts. Prior work reports cumulative-best metrics, which are non-decreasing by construction, so the raw per-iteration security trajectory has never been examined for IaC. Aims: We study security regression (a previously-passing CIS Benchmark check that fails after a repair iteration) to determine whether and how often iterative LLM repair degrades security while fixing other issues. Method: We analyze 5,968 scenario timelines from the IaC-Eval benchmark, each one scenario run through one configuration for up to 5 repair iterations. The 15 configurations (six model-specific RAG, nine model-aggregated non-RAG, three temperatures each) yield 4,440 iteration transitions with Checkov data on both sides. We track 30 individual CIS check IDs and classify root causes from code diffs, under two detection modes: standard (inclusive) and strict (exclusive check failures only). Results: Under standard detection, 13.8% of scenarios (24.8% of transitions) exhibit at least one regression. Under strict detection the rate falls to 3.3% of scenarios (5.2% of transitions), indicating most apparent regressions are multi-resource measurement artifacts. Resource restructuring (79.0%) is the dominant root cause. Regression transitions show 2.6x more code churn (Cohen's d=0.90) and 4.9x higher strict-mode check volatility (d=1.49). Of standard-mode regressions, 36.6% self-correct within an average of 1.2 iterations; iteration 3 is the optimal stopping point. Conclusions: Iterative IaC repair does introduce security regressions, but the conservative, defensible rate is about 3.3% of scenarios. Our findings motivate security-aware feedback-loop design and actionable iteration-budget guidance.

</details>


### [20] [LLM-Assisted Dynamic Threat Analysis for Attacker-Reachable Software Weaknesses in Autonomous Vehicles](https://arxiv.org/abs/2608.13450)
*Md Wasiul Haque,Sagar Dasgupta,Mizanur Rahman,Md Rayhanur Rahman*

Main category: cs.SE

TL;DR: 研究利用大语言模型（LLM）自动化自动驾驶系统 Autoware 的漏洞动态验证过程，重点探讨了 LLM 生成测试用例时面临的挑战。


<details>
  <summary>Details</summary>
Motivation: 自动驾驶软件栈规模庞大且安全至关重要，静态分析虽能识别潜在漏洞，但手动构建可执行的动态验证测试用例（artifact）极其困难。

Method: 通过对 Autoware 进行编译器精确的静态分析识别漏洞点，并利用本地开源 LLM 生成测试 harness，结合编译器反馈进行修复，最后通过模糊测试（fuzzing）验证。

Result: LLM 生成的测试用例在编译阶段面临巨大挑战，80% 的编译失败源于依赖配置而非逻辑问题；推理模型虽首次编译成功率较高，但其触发的崩溃均源于桩函数（stub）而非系统本身。

Conclusion: 在自动驾驶软件栈的动态分析中，构建系统的集成（build integration）是比漏洞生成或模糊测试更大的核心障碍。

Abstract: Autonomous vehicles depend on large safety-critical software stacks, where weaknesses reachable from adversarial inputs may affect steering, braking, or other control decisions. Static analysis can identify candidate sites, but dynamically confirming exploitability requires executable test artifacts that are difficult to construct manually. We investigate whether large language models (LLMs) can automate this process for Autoware, an open-source autonomous-driving stack. We perform compiler-precise static analysis across 185 packages, identifying 1,375 decision rules, 2,274 validation checks, and 482 input-to-safety-output flows, from which we derive a weakness taxonomy and sample 740 reachable sites. Two local open-weight LLMs, a no-static-context ablation, and a naive-template baseline generate 3,700 artifact sets, which are compiled against the real build under sanitizers, repaired through compiler-in-the-loop feedback, and fuzzed when executable. The main result is a build-integration failure taxonomy showing that 80% of first-shot compilation failures arise from dependency wiring rather than program logic. The reasoning model compiled 64% of harnesses on the first attempt, compared with 6% for the code-specialized model. Repair achieved full object-compileability for the reasoning model only through extensive stubbing; fewer than half of its harnesses reached the fuzzer, and all 37 observed crashes originated in stubbed code rather than Autoware. No candidate weakness was dynamically confirmed within budget. These results show that build integration, not candidate generation or fuzzing, is the primary barrier to reliable LLM-assisted dynamic analysis of full autonomous-vehicle software stacks.

</details>


### [21] [CAPRI: Contract-Aware Proof Repair for Isabelle](https://arxiv.org/abs/2608.13459)
*Jim Woodcock,Gabriel Leite,Augusto Sampaio,Ran Wei*

Main category: cs.SE

TL;DR: 本文提出CAPRI—a面向Isabelle证明的合同感知修复工作流，通过机器可读编辑合同约束LLM修改范围，并在12个失败证明上评估5种工作流（180次运行），结果显示仅允许修改证明体的接口可避免合同违规，同时保持高修复成功率。


<details>
  <summary>Details</summary>
Motivation: 大型语言模型（LLMs）在Isabelle证明发现中具备潜力，但Isabelle仅验证理论是否被接受，并不保证LLM仅修改开发者授权的内容。因此，需要一种机制来约束LLM的编辑行为，确保其修改符合预定义的合同要求，避免对受保护代码的意外修改。

Method: 作者设计了CAPRI工作流，其中Isabelle负责验证证明的正确性，同时一个独立的检查器强制执行机器可读的编辑合同。该工作流记录了提示、建议、候选仓库、诊断、判决和哈希值以供审计。他们在四个开发项目的12个失败证明上评估了五种工作流，每种任务和条件下进行了3次复制实验，共进行180次运行并获得138次有效修复。此外，他们还进行了一次事后OpenRouter实验，以及在Sol配置和OpenAI Responses条件下比较了不同演示对修复效果的影响。

Result: 在144个由Isabelle接受的终端候选方案中，有6个修改了受保护文本，但这些都出现在可以编辑完整理论的迭代式工作流中。仅允许修改证明体的接口成功修复了29/36的有效证明，且未违反任何合同；而对应的全理论工作流成功修复了31/36。一次性修复（one-shot）完成了22/36的修复，而后续的预冻结迭代工作流完成了32/36的修复。Sol配置与OpenAI Responses条件下的修复率分别为33/36和29/36，但 McNemar检验显示其差异不具统计学显著性（p=0.0625）。

Conclusion: CAPRI工作流通过合同感知机制有效控制LLM在Isabelle证明修复中的行为，其中限制编辑范围仅含证明体的方式能够在避免违反编辑合同时维持较高的修复成功率，这为安全、可审计的LLM辅助证明构建提供了可靠实践。

Abstract: We address the use of large language models (LLMs) to help discover Isabelle proofs. An Isabelle build establishes that the submitted theory is accepted, but not that an LLM changed only what the developer authorised. We present CAPRI, a contract-aware repair workflow in which Isabelle checks the proof and an independent checker enforces a machine-readable edit contract. Prompts, proposals, candidate repositories, diagnostics, verdicts, and hashes are retained for audit. We evaluate five workflows on twelve failed proofs from four developments, with three replicates per task and condition, giving 180 runs and 138 valid repairs. Of 144 terminal candidates accepted by Isabelle, six had modified protected text; all arose in iterative workflows that could edit a complete theory. A proof-body-only interface produced 29/36 valid repairs and no contract violations, compared with 31/36 for the corresponding full-theory workflow. One-shot repair produced 22/36, while a later prospectively frozen iterative workflow produced 32/36; these figures compare complete workflows rather than individual mechanisms. A separate post hoc OpenRouter campaign found no improvement in the designated Luna comparisons. A Sol configuration with matched demonstrations produced 33/36 repairs, compared with 29/36 in the frozen OpenAI Responses condition, but the difference was not statistically significant in a one-sided exact McNemar test ($p=0.0625$).

</details>


<div id='cs.PF'></div>

# cs.PF [[Back]](#toc)

### [22] [Performance Reporting of Mathematical Library Installations with LAAB - An Overview](https://arxiv.org/abs/2608.13512)
*Aravind Sankaran,Paolo Bientinesi*

Main category: cs.PF

TL;DR: 本文介绍了一个名为LAAB（线性代数感知基准测试）的框架，用于系统评估和报告HPC系统上数学库的性能，聚焦于可追溯性、兼容性、可靠性和可访问性四大目标。


<details>
  <summary>Details</summary>
Motivation: 数学库是科学应用程序计算构建块的关键接口，准确报告其性能对于评估应用程序效率、估算计算时间需求和编制资源分配申请至关重要。然而，现有性能报告存在可追溯性（与具体库安装和执行设置的关联）、兼容性（与高层科学应用的关联）、可靠性（测量变异性下的解释支持）以及可访问性（报告、基准定义和元数据的可用性）等方面的挑战。

Method: 作者定义了性能报告的四大目标，并据此设计了LAAB框架。该框架通过结构化的设计来解决上述挑战，确保每个报告都能追溯到确切的库安装和执行设置，同时将库操作性能与使用它们的高层科学应用联系起来，并在测量变异性存在时提供可靠的解释支持，最终使报告、基准定义和相关元数据便于检查和复现。

Result: LAAB框架的设计能够有效应对性能报告中的四大挑战。尽管摘要中未提供具体数值结果，但该框架通过其结构化方法增强了报告的可追溯性、兼容性、可靠性和可访问性，从而提升了数学库性能评估的科学性和可复现性。

Conclusion: LAAB框架为HPC系统上的数学库性能评估和报告提供了一个系统化、可靠且可复现的方法论，有助于更准确地评估应用程序效率、估算计算资源需求，为资源分配决策提供支持。

Abstract: We present the Linear Algebra Aware Benchmarks (LAAB) framework for systematically assessing and reporting the performance of mathematical library installations on HPC systems. Mathematical libraries provide interfaces for operations that form the computational building blocks of scientific applications. Reporting their performance is important for assessing application efficiency, estimating compute-time requirements, and preparing resource-allocation requests. In this paper, we define four objectives for performance reporting: 1) traceability, linking each report to the exact library installation and execution settings; 2) compatibility, relating library-operation performance to higher-level scientific applications that use them; 3) reliability, supporting interpretation in the presence of measurement variability; and 4) accessibility, ensuring that reports, benchmark definitions, and relevant metadata are available for inspection and reproduction. We then present the design of LAAB and show how it addresses the challenges associated with these objectives.

</details>


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [23] [Offering Microsecond-Scale Cross-VM Core Elasticity on Colocated Lightweight Virtual Machines](https://arxiv.org/abs/2608.12633)
*Yibo Yan,Seo Jin Park*

Main category: cs.DC

TL;DR: HyperFlux 是一种基于 KVM 的超轻量 VM 基底，允许实时调整 VM 的物理核心数量（并行宽度），从而平衡多租户共享时的高密度与低延迟需求。它能在微秒级动态分配核心，显著优于传统方法。


<details>
  <summary>Details</summary>
Motivation: 现有的虚拟机基底在保障尾延迟时缺乏实时核心迁移能力。过度配置会降低密度，而 vCPU 热插拔延迟高、无需多核支持。需要一种低开销、快速启动且支持弹性核心的解决方案。

Method: HyperFlux 通过在 KVM 内实现动态核心调度机制，利用虚拟与物理内核映射，技术关键点包括：（1）低延迟核心协商机制（13μs 动态分配）和（2）与核心重定向相关的协议优化。同时通过只加载必要内核模块保持镜像大小最小化。

Result: 对比实验显示：与 Firecracker（无动态核心调整）和 Cloud Hypervisor（热插拔微秒级改进）相比，HyperFlux 在多租户场景下高负载时高优先级工作负载的 P99 尾延迟可降低近 10 倍；单 VM 内存占用 3.2MB、冷启动 1.37ms 与超轻量 VM 按照性能，且保留多核支持。

Conclusion: HyperFlux 在保持最快启动速度和低内存足迹的同时，首次实现微秒级核心迁移能力，为高密度云环境中的低尾延迟服务提供了实践解决方案，但需要兼容 KVM 的基础设施支持

Abstract: Serverless platforms commonly colocate many diverse workloads, each in a fast-booting, memory-lean virtual machine (VM), to improve deployment density. Overprovisioning each VM for its peak protects tail latency during traffic bursts but hurts density; maintaining high density while effectively protecting tail latency requires the infrastructure to be able to shift physical cores, at a microsecond timescale, to whichever latency-sensitive VM is bursting and reclaim them as the burst subsides. No VM substrate delivers this: conventional VMs resize a guest's cores only through a millisecond-scale vCPU hot-plug path, Firecracker fixes a VM's core count at boot, and the ultralight VMs that boot fastest drop multicore execution entirely.
  We present HyperFlux, a commodity-KVM ultralight VM substrate that makes a VM's parallelism width (the number of physical cores backing it) elastic at runtime. We show that HyperFlux can move a core across VMs in merely 13$μ$s, even when forcibly reclaiming it from a busy donor, orders of magnitude faster than vCPU hot-plug. A HyperFlux VM incurs only a 3.2MB memory footprint and can cold-boot in 1.37ms, on par with the fastest-booting ultralight VMs, while uniquely supporting multicore parallelism. Under colocation, it can reduce high-priority VMs' tail latency by up to 10x under high load compared to static core-sharing with Firecracker and Cloud Hypervisor, and deliver a lower and more stable tail latency compared to using cgroup and vCPU hot-plug under changing load bursts.

</details>


### [24] [InFactPlanner: Planning Sustainable Geo-Distributed LLM Data Centers](https://arxiv.org/abs/2608.12915)
*Nicoletta Tsiopani,Moysis Symeonides,George Pallis,Marios D. Dikaiakos*

Main category: cs.DC

TL;DR: 本文介绍了 InFactPlanner，这是一个基于追踪驱动的决策支持框架，旨在为大语言模型（LLM）推理的持续服务提供可持续性“假设分析”（what-if analysis），帮助运营商在基础设施建设前评估不同部署方案对能源、碳排放和水资源消耗的影响。


<details>
  <summary>Details</summary>
Motivation: 随着 LLM 推理需求的快速增长，可持续性关注点已从一次性训练转向持续的推理服务。目前的挑战在于，在实际构建大规模基础设施之前，直接测量部署方案的资源消耗既昂贵、缓慢且有时不可行。

Method: InFactPlanner 结合了查询追踪（query traces）、硬件模型配置文件、候选站点配置、PUE/WUE 参数、可再生能源生成模型以及随时间变化的电网碳强度，将低层级的服务效应抽象为可配置的硬件模型，从而实现对电力、能源、碳排放、水资源使用、延迟和服务器利用率的快速评估。

Result: 该框架的能源核算管道验证结果与参考 LLM 推理能量估计的偏差小于 10%；它在多个数据中心和服务器规模上验证了可扩展性；并演示了硬件选择、可再生能源集成、地理部署和碳感知路由等场景驱动的决策分析。

Conclusion: 研究表明，追求可持续性的最优选择可能与追求延迟最优的选择存在显著差异，且部署的碳价值在很大程度上取决于当地的电网结构。

Abstract: The rapid growth of LLM inference is shifting sustainability concerns from one-time training to continuous serving, where infrastructure decisions shape energy use, carbon emissions, water consumption, and service quality. Yet operators often need to compare deployment alternatives before large-scale infrastructure is built, making direct measurement costly, slow, and sometimes infeasible. We present InFactPlanner, a trace-driven decision-support framework for what-if analysis of sustainable AI data center deployment for LLM inference across single and geo-distributed sites. InFactPlanner combines query traces, hardware-model profiles, candidate site configurations, PUE/WUE parameters, renewable generation models, and time-varying grid carbon intensity to estimate power, energy, carbon emissions, water use, latency, and server utilization. The framework abstracts low-level serving effects into configurable hardware-model profiles, enabling rapid comparison of site selection, capacity placement, hardware, model, renewable integration, and routing choices. We validate the energy accounting pipeline by reproducing reference LLM inference energy estimates with less than 10% deviation, evaluate scalability across multiple data centers and server counts, and demonstrate scenario-driven decision analyses for hardware selection, renewable placement, geographic deployment, and carbon-aware routing. Our results show that sustainability-optimal choices can differ from latency-optimal ones, and that the carbon value of deployment depends strongly on the local grid mix.

</details>


### [25] [Efficient Randomized LL/SC that Preserves History Independence](https://arxiv.org/abs/2608.12946)
*Dante Bencivenga,Homa Habashi,Philipp Woelfel*

Main category: cs.DC

TL;DR: 通过随机化与FADD，实现LL/SC对象的空间复杂度降低至O(nτ+m)，解决了确定性算法的O(n²τ+m)空间问题，并推动了QHI哈希算法的可行性。


<details>
  <summary>Details</summary>
Motivation: LL/SC对象在硬件上不可用，其实现方案影响并发算法的效率，尤其是QHI算法，需支持历史无关性和低开销。

Method: 结合随机化和FADD（浮点加法）操作，降低基础对象的数量要求，同时保持线性化和可预测性。

Result: 在弱自切换对手下，空间复杂度达到O(nτ+m)，当m=O(1)时与已知下界匹配，且支持QHI算法无渐进复杂度增加。

Conclusion: 该研究展示了使用现有硬件实现LL/SC对象的可行性，为效率高的并发数据结构提供了新工具。

Abstract: We study the fundamental problem of implementing $m$ linearizable LL/SC objects with constant expected step complexity in a system of $n$ processes, using bounded base objects commonly available in hardware. Assuming that each process may have at most $τ$ outstanding LL operations, the best known deterministic algorithm requires $Ω(n^2τ+ m)$ base objects (CAS and registers) [Blelloch and Wei, DISC 2020]. Previously, no comparable randomized algorithm was known.
  By employing randomization and FADD in addition to CAS and registers, we obtain a space bound of $O(nτ+m)$ against the weak adaptive adversary. For $m=O(1)$ this matches a lower bound for algorithms using CAS and registers [Aghazadeh and Woelfel, PODC 2015].
  In addition, our object can be employed by quiescently history-independent (QHI) algorithms: Whenever no operation on the object is pending and no process has an outstanding LL operation, its internal memory state is uniquely determined by the values of the $m$ LL/SC objects. An important application is a QHI dynamic hashing algorithm presented at STOC 2025, which uses $Θ(m)$ hardware LL/SC objects to maintain a hash table of size $m$ [Attiya, Bender, Farach-Colton, Oshman, and Schiller, STOC 2025]. But LL/SC is not available in hardware, and prior to our work no wait-free or efficiently lock-free software implementation of LL/SC with similar properties was known. Our work demonstrates that one can actually implement the hashing algorithm on available hardware, without an asymptotic increase in step and space complexity, under the reasonable assumption that $m=Ω(n)$.

</details>


### [26] [TEMPO: Makespan-Aware Expert-Parallel Load Balancing Across Memory- and Compute-Bound Regimes](https://arxiv.org/abs/2608.13057)
*Jie Li,Chenxin Jia,Jinliang Shen,Cunzhuang Liu,Ruiyi Ding,Jianwen Xian,Kang He,Chengru Song*

Main category: cs.DC

TL;DR: 专家并行（EP）MoE服务中，所有层同步于最慢GPU。现有负载均衡方法（EPLB、LPLB、UltraEP、METRO）假设专家耗时线性依赖于token数，但实测并非如此：在较低token数下，HBM权重流 dominates，耗时与激活副本数相关；在较高token数时，分组GEMM将token向128的M-tile对齐，导致分割专家增加填充计算。


<details>
  <summary>Details</summary>
Motivation: 现有EP MoE调度器假设专家执行时间线性依赖于token数量，从而进行负载均衡。然而，这一假设在真实的GPU硬件上并不成立，导致调度效率低下，无法达到最优性能。

Method: 作者在两个数据中心GPU生成平台上进行了实验性测量，发现专家执行时间呈现双重 regimes。他们提出了一种基于max-affine模型$t=\max(a+bG,c+βN)$来描述该现象，并将每个批次的专家调度形式化为固定成本的完工时间（makespan）问题。基于此，设计了一种名为的调度器，结合SGLang框架，在图外进程中运行，并将调度与计数收集融合为一个内核。

Result: 基于8-GPU Testbed A的微基准测试 results 表明，始终在最佳固定 baselines附近1%范围内，並在regimes混合时提升多达15.5%。在Testbed B的端到端测试中，Qwen3-235B模型获得4%~6%的throughput提升，p99延迟降低约15.6%。而DeepSeek-V3（处于通讯主导区间）仅展现出机制 cost。

Conclusion: 专家执行时间与token数的非线性关系显著影响EP MoE系统性能。当前的线性均衡策略在不同 regimes下存在显著劣势，无法统一优化。本文提出了一种新的调度方法，并在实际部署中验证了其有效性，证明了根据动态 regimes自适应优化调度的必要性。

Abstract: In expert-parallel (EP) MoE serving, every layer synchronizes at the slowest GPU. Dispatchers balance token counts (EPLB, LPLB, UltraEP) or activated-expert counts (METRO), assuming expert time is linear in one. Measurements on two datacenter GPU generations show it is neither: below $\nstar\!\approx\!156$--$168$ tokens, HBM weight streaming dominates---cost attaches to \emph{activated replicas}, not tokens; above it, grouped GEMM rounds tokens to 128-tile $M$-tiles, so \emph{splitting} an expert adds padded compute. A max-affine profile $t=\max(a+bG,\,c+βN)$ captures both regimes. Realistic decode batches hold hot experts in the linear regime and cold in the flat \emph{simultaneously}; recorded batches show proxy dispatches differ by $1.4$--$1.6\times$ in modeled block time (p95 up to $1.7\times$), and \emph{which} proxy wins flips with the regime. We formalize per-batch dispatch as a fixed-charge makespan problem---NP-hard on two fully replicated GPUs, polynomial in degenerate limits---and present \sys{}, a makespan-aware dispatcher solving it in milliseconds off the critical path; its SGLang integration runs out-of-process and fuses dispatch with count collection into one in-graph kernel. Anchored by an 8-GPU Testbed~A microbenchmark, \sys{} stays within 1\% of the best fixed baseline everywhere and wins by up to $15.5\%$ where regimes mix. End-to-end on Testbed~B, Qwen3-235B (inside the win region) gains $4$--$6\%$ throughput and cuts p99 latency by ${\sim}15.6\%$; DeepSeek-V3 (outside, communication-dominated) shows only mechanism cost. A phase diagram, not a universal win, is the claim: it predicts both outcomes before deployment.

</details>


### [27] [Meshlib: In-Process Policy Enforcement for Sidecar-less Service Meshes](https://arxiv.org/abs/2608.13107)
*Habib Mostafaei,Tom van Liempd*

Main category: cs.DC

TL;DR: Meshlib是一种基于Cilium的无Sidecar服务网格扩展方案，通过将L7策略集成到应用进程中来消除Sidecar带来的延迟和资源开销。


<details>
  <summary>Details</summary>
Motivation: 传统的Sidecar模式在微服务架构中会因重复的应用层解析而引入显著的延迟和资源消耗。

Method: 提出Meshlib架构，利用非侵入式应用绑定库在应用进程内执行L7策略，并结合Cilium的eBPF数据面处理传输层身份验证和路由。

Result: 在TrainTicket基准测试中，Meshlib在执行复杂安全策略时表现出比Istio、Linkerd等方案更低的端到端延迟，且资源开销相当。

Conclusion: Meshlib成功实现了在不牺牲安全语义的前提下消除Sidecar，并支持与现有未修改服务的平滑集成。

Abstract: Service meshes facilitate service-to-service communication and enforce security policies in microservice architectures. However, they often depend on per-pod sidecar proxies, which introduce significant latency and resource overhead due to redundant application-layer parsing on every request. Eliminating sidecars without compromising security guarantees remains a central challenge. To address this, we introduce Meshlib, a sidecar-less service mesh extension built on Cilium as a control-plane extension. Meshlib incorporates a non-intrusive application-bound library that enforces Layer-7 policies within the application process, while delegating transport-level identity and routing to Cilium's eBPF-based data plane. This separation of responsibilities removes sidecar-induced latency and maintains the security semantics of the service mesh. The architecture remains fully interoperable with unmodified services, enabling incremental adoption within existing deployments. We evaluate Meshlib against Istio, Linkerd, and unmodified Cilium on the TrainTicket benchmark, enforcing 126 security policies across 37 services and show that it achieves the lowest end-to-end latency of all evaluated configurations with comparable resource overhead.

</details>


### [28] [Validation-Centric AI-Assisted GPU Porting of a 250,000+ Line Legacy Weather Simulation Code](https://arxiv.org/abs/2608.13122)
*Tetsuya Hoshino,Masaya Kato,Kazuhisa Tsuboki,Daichi Mukunoki,Takahiro Katagiri,Toshihiro Hanawa*

Main category: cs.DC

TL;DR: 该论文提出了一种以验证为中心的AI代理GPU迁移工作流程，通过对Fortran天气模拟代码CReSS进行案例研究，在保持科学有效性的前提下实现了5.1倍应用级加速，生成162个经过验证的GPU内核。


<details>
  <summary>Details</summary>
Motivation: 传统GPU迁移面临科学有效性保障难题，尤其是针对25多万行的遗留科学代码。作者意识到，科学应用不仅是旧代码库，更是通过长期发展积累的可信赖资产。当前AI代理方法在发现5个因浮点运算差异导致的内核问题方面证明有价值，这些问题包含分支分支的阈值敏感性和消元效应，为开发者提供了针反馈。

Method: 工作流程包括：AI代理提取OpenMP区域，从物理上有意义的仿真状态生成基于转储的内核基准，使用OpenACC转换并通过元素级比较验证转储参考数据。方法要求管理跨会话的上下文、运行时状态重构以及抵御静态分析遗漏的成本 recovery。

Result: 在实际台风模拟期间，工作流程为162个目标内核生成经过数值验证的GPU实现，实现了5.1倍的应用级加速，同时检测到5个因浮点和内核函数差异导致的数值不一致问题。

Conclusion: AI代理GPU迁移不仅需要代码生成能力，更需要验证导向的工作流程设计。对于需要基于转储验证的大型遗留科学应用，必须管理会话范围的上下文、运行时状态重建以及成本控制的恢复机制。

Abstract: Recent advances in large language models have made CLI-based AI agents a practical tool for accelerating GPU porting of large legacy scientific applications. Such applications, however, are not merely old code bases; they are scientific assets whose credibility has been accumulated through long-term development, comparison with observations, and use in domain studies. GPU porting must therefore preserve this scientific validity while adapting the implementation to GPU-centric HPC systems. This paper presents a validation-centric AI-assisted GPU porting workflow through a case study of CReSS, a legacy Fortran weather simulation code with more than 250,000 lines. The workflow uses an AI agent to extract OpenMP regions, generate dump-based kernel benchmarks from physically meaningful simulation states, apply OpenACC transformations, and validate results through element-wise comparison with dumped reference data and application-level validation. Using a real typhoon simulation, the workflow produced numerically validated GPU implementations for 162 target kernels and achieved a 5.1x application-level speedup within practical wall-clock development cost. In particular, it detected numerical discrepancies in five kernels caused by floating-point and intrinsic-function differences, including threshold-sensitive branch divergence and cancellation effects, enabling feedback to the application developers. The case study suggests that, for large legacy scientific applications requiring dump-based validation, practical AI-assisted GPU porting must manage session-spanning context, runtime-state reconstruction, and costly recovery from small static-analysis omissions. These findings demonstrate that AI-assisted GPU porting requires not only code generation, but validation-centric workflow design.

</details>


### [29] [LipCache: A Local Inference Proxy with Certified Caching for Edge Image Classification Service](https://arxiv.org/abs/2608.13144)
*Zhengzhe Xiang,Yinlin Chen,Fuli Ying,Binbin Zhou,Hailiang Zhao,Schahram Dustdar*

Main category: cs.DC

TL;DR: LipCache是一个边缘 vision 推理缓存框架，通过引入轻量级GuardNet并施加Lipschitz约束，将缓存命中从经验阈值测试转化为具有理论边界的几何认证决策，显著提升了缓存可靠性。


<details>
  <summary>Details</summary>
Motivation: 随着边缘视觉服务向低延迟高吞吐场景发展，如何在不牺牲可靠性的前提下降低视觉模型的推理成本成为核心挑战。传统语义缓存方法依赖经验相似阈值，虽提升命中率，却易在决策边界附近引入静默误分类。

Method: LipCache无需修改现有部署的主模型MainNet，引入轻量网络GuardNet将输入映射到受Lipschitz约束限制的低维特征空间，基于局部分类间隔和分类头的谱范数计算每个样本的认证复用半径。运行时仅当查询特征位于认证复用球内时才 reusing 缓存结果，否则回退到MainNet，将缓存命中从经验阈值测试转化为具明确理论边界的几何认证决策。

Result: 在CIFAR、Tiny-ImageNet和SVHN等标准图像分类任务上，LipCache实现最高达1.65倍的推理加速，同时维持有限的端到端准确率下降，且所有接受的缓存命中均满足GuardNet侧的认证一致性条件。

Conclusion: LipCache证明了按样本认证复用可以在降低主模型 fallback 频次的同时保持理论一致性，为边缘可靠缓存加速推理提供了可行方案。

Abstract: As edge-side vision services continue to expand toward low-latency, high-throughput scenarios, reducing the inference cost of vision models without sacrificing reliability has become a central concern. Existing semantic caching methods largely rely on empirical similarity thresholds; while such thresholds improve hit rates, they tend to introduce silent misclassifications near decision boundaries. To address this, we propose \texttt{LipCache}, a certified semantic caching framework for image classification. Without modifying the existing deployed main model, \texttt{MainNet}, the framework introduces a lightweight network, \texttt{GuardNet}, that maps inputs into a low-dimensional feature space subject to a Lipschitz constraint. It then computes a per-sample certified reuse radius from the local classification margin and the spectral norm of the classification head. At runtime, a cached result is reused only when the query feature falls inside the certified reuse ball; otherwise, the query falls back to \texttt{MainNet}. Thus, cache hits are transformed from empirical threshold tests into geometric certification decisions with explicit theoretical boundaries. Across standard image classification tasks like CIFAR, Tiny-ImageNet, and SVHN, \texttt{LipCache} achieves a measured speedup of up to $1.65\times$ with limited end-to-end accuracy degradation, while all accepted cache hits satisfy the \texttt{GuardNet}-side certified-consistency condition. Furthermore, an enhanced \texttt{GuardNet} training recipe substantially improves cache hit rates in the Tiny-ImageNet multi-class extension while maintaining a certified-consistency rate of $100\%$. These results demonstrate that per-sample certified reuse can reduce main-model fallback while preserving theoretical consistency, providing a feasible approach to reliable cache-assisted inference at the edge.

</details>


### [30] [Triangle-Free Coloring in LOCAL via Resilient Lovász Local Lemma](https://arxiv.org/abs/2608.13357)
*Peter Davies-Peck,Xusheng Zhang*

Main category: cs.DC

TL;DR: 本文通过引入 Davies [SODA 2023] 的「弹性」定义，改进 Pettie-Su 的分布式三角形免色算法，使得生成的 Lovász 局部 lemma（LLL）实例可在 $\log^{O(1)}\log n$ 回合内求解。这使得整体算法复杂度从原有的 $O(k+\log^* n)$ 降至 $O(k)+\log^{O(1)}\log n$，消除 LLL 成为瓶颈，并获得三角形免图用 $o(\Delta)$ 颜色的首个 $\log^{O(1)}\log n$ 回合算法，并配套给出基数为 5 的图配色算法，匹配最佳存在性上界。


<details>
  <summary>Details</summary>
Motivation: 分布式图论中，Lovász 局部 lemma 是处理局部可检查标记问题的核心工具，尤其用于稀疏结构图的颜色分配。三角形免图配色是亚对数回合算法的基准问题，但现有分布式 LLL 求解算法需 $O(\log_{\Delta} n)$ 或 $O(\Delta/\log\Delta)$ 回合，成为阻碍算法突破瓶颈的关键限制。

Method: 本文利用 Davies [SODA 2023] 提出的「弹性」定义，重新改编 Pettie-Su 的算法框架，使得生成的 LLL 实例可以在 $\log^{O(1)}\log n$ 回合内高效求解。当参数 $k = \log^{\omega(1)}\log n$ 时，无需进行 LLL 采样步骤，从而使 LLL 计算不再是算法复杂度的主导部分，整体复杂度降至 $O(k)+\log^{O(1)}\log n$。

Result: 作为推论，首次获得三角形免图用 $o(\Delta)$ 颜色的 $\log^{O(1)}\log n$ 回合分布式着色算法。同一框架还推广至基数为 5 的图，使用 $(1+\varepsilon)\Delta/\ln\Delta$ 颜色，在 $O(k)+\log^{O(1)}\log n$ 回合内完成，与最佳存在性上界相匹配。

Conclusion: 通过引入弹性定义，成功将分布式 LLL 的计算成本降为对数阶，突破了之前的回合复杂度瓶颈，为子对数回合的分布式图颜色分配提供了新的算法范式，特别是在三角形免图和高基数图类上的显著改进。

Abstract: The Lovász Local Lemma (LLL) is a probabilistic tool that has been shown to be of central importance in the study of distributed algorithms. For example, the constructive LLL is known to be complete for the class of locally-checkable labeling problems with $o(\log n)$ randomized complexities in the LOCAL model. One classic application of the LLL is in coloring graphs with some sparse structure, such as triangle-free graphs. Triangle-free coloring therefore serves as a benchmark problem for techniques for sublogarithmic randomized distributed algorithms. The state-of-the-art distributed triangle-free coloring algorithm of Pettie and Su [ICALP 2013, Information and Computation 2015] uses $\fracΔ{k}$ colors (where $k$ can be up to $(\frac14 - \varepsilon)\ln Δ$) and consists of $O(k+\log^* n)$ applications of the distributed LLL. However, the distributed LLL is itself a difficult problem; despite significant study, the fastest algorithms known require $O(\log_Δn)$ or $O(\fracΔ{\logΔ})+\log^{O(1)}\log n$ rounds. In this work, we adapt the Pettie-Su's algorithm so that the resulting LLL instances can be solved in $\log^{O(1)}\log n$ rounds, by employing the 'resilience' definition of Davies [SODA 2023]. This gives an $O(k)+ \log^{O(1)}\log n$ complexity (since the LLL is not needed when $k= \log^{ω(1)}\log n$), essentially causing the LLL steps to no longer be the bottleneck of the algorithm. As a corollary we obtain the first $\log^{O(1)}\log n$-round algorithms for coloring triangle-free graphs with $o(Δ)$ colors. The same framework also yields a companion girth-$5$ algorithm, using $(1+\varepsilon)Δ/\ln Δ$ colors in $O(k)+ \log^{O(1)}\log n$ rounds, matching the best known existential upper bound for the number of colors.

</details>


### [31] [Fast Tendermint: Speeding Up a Foundational Consensus Protocol](https://arxiv.org/abs/2608.13434)
*Preston Vander Vos,Daniel Cason*

Main category: cs.DC

TL;DR: Fast Tendermint 使在 $f < n/5$ 条件下，协议可在好情况下仅两步通信完成，同时保留原始的领袖轮换结构。


<details>
  <summary>Details</summary>
Motivation: 受低延迟区块链需求驱动，现有部分同步 BFT 协议需三步通信，研究表明在 $f < n/5$ 时可降至两步。

Method: 通过合并 prevote 与 precommit 为单一投票步骤，并将 locked 与 valid 状态合并，构建 Fast Tendermint 并在 Quint（TLA+ 语法）中正式化规格。

Result: 提供了同意、有效性和终止的正式证明，并通过模型检查验证了协议的正确性。

Conclusion: 证明在 $n > 5f$ 设置下，保持领袖轮换的同时实现两步决策是可行的，为更高效的 BFT 共识提供新思路。

Abstract: Tendermint is among the most widely studied and deployed Byzantine fault-tolerant (BFT) consensus protocols, owing in part to its native leader-rotation mechanism that subsumes complex view changes. Like most partially-synchronous BFT protocols, Tendermint tolerates $f < n/3$ Byzantine processes and decides in three communication steps. Motivated by the push for lower-latency blockchains, a recent line of work shows that consensus can be solved in two communication steps when $f < n/5$. We present Fast Tendermint, an adaptation of Tendermint to the $n > 5f$ setting that decides in two communication steps in the good case, while preserving Tendermint's leader-rotation structure. Fast Tendermint collapses Tendermint's prevote and precommit steps into a single voting step and merges the $locked$ and $valid$ state. We give proofs of agreement, validity, and termination, and a formal specification in Quint, a modern surface syntax for TLA+, used to model-check the protocol.

</details>


### [32] [OpScale: Operator-level Provisioning and Autoscaling for LLM Serving](https://arxiv.org/abs/2608.13499)
*Xingqi Cui,Chieh-Jan Mike Liang,Ziang Tang,Jiarong Xing,Haoran Qiu*

Main category: cs.DC

TL;DR: OpScale is a framework for operator-level autoscaling in LLM serving systems, offering finer granularity than existing monolithic model-level approaches, leading to improved efficiency and performance.


<details>
  <summary>Details</summary>
Motivation: Unmet challenge of balancing cost efficiency and strict SLOs in cloud GPU clusters serving LLMs, driven by the inefficiency of coarse-grained (model-level) autoscaling.

Method: Proposal of OpScale, a practical operator-level orchestration framework addressing complexity and space limitations through profiling, provisioning, placement, and runtime optimizations.

Result: Offers significant gains: up to 36.3% fewer GPUs, 28% less power, or 44% higher throughput under fixed cost budgets, validated with large-scale production traces.

Conclusion: Operator-level autoscaling outperforms traditional model-level approaches, demonstrating substantial improvements in resource utilization and system efficiency for LLM serving.

Abstract: Achieving cost efficiency while meeting strict user-facing SLOs (e.g., time-to-first-token) remains a fundamental challenge for cloud GPU clusters serving large language models (LLMs). Autoscaling is the key mechanism for cluster resource management, yet a basic system design question is open for serving LLMs: what should be the unit of scaling? Existing approaches primarily treat the entire model as a monolithic scaling unit--simple but unable to capture the fine-grained dynamics of inference workloads. As a result, such coarse-grained scaling often leads to either SLO violations under bursty demand or significant GPU under-utilization.
  Our characterization reveals substantial operator heterogeneity, exposing operator-level elasticity as a viable scaling primitive. We present OpScale, a practical operator-level orchestration framework of profiling, provisioning, placement, and runtime serving. OpScale is designed to tackle the high complexity and the space explosion problem, arising from operating at this finer granularity. Evaluated with production traces on up to 40 A100s and 24 GB200s, OpScale attains SLOs with up to 36.3% fewer GPUs and 28% less power, or achieves 44% higher throughput under fixed cost budgets.

</details>


<div id='cs.AR'></div>

# cs.AR [[Back]](#toc)

### [33] [SCALE-Sim EVA: Design Principles for an Extensible, Visualizable, and Adaptable Accelerator Simulation Framework](https://arxiv.org/abs/2608.12354)
*Jingtian Dang,Ritik Raj,Tushar Krishna*

Main category: cs.AR

TL;DR: 本文介绍了 SCALE-Sim EVA，一个用于加速器建模的可扩展、可视化且可适配的 IR 感知仿真框架。


<details>
  <summary>Details</summary>
Motivation: 现代 AI 加速器具有异构计算单元和层级存储结构，传统的固定加速器模拟器难以扩展并适应不断变化的执行模型。

Method: EVA 将工作负载表示为基于张量的命令，通过命令分解机制衔接编译器 IR 与硬件执行，并基于张量就绪状态、硬件可用性和延迟来计算周期级定时，支持可组合的硬件组件和用户定义的行为。

Result: 该框架能够模拟复杂的硬件层级结构，并能通过开放的命令与存储轨迹实现对执行时间线、内存占用和资源竞争的可视化分析。

Conclusion: EVA 为现代异构 AI 加速器的仿真提供了一种灵活、可扩展且面向编译器表示的建模方法。

Abstract: Modern AI accelerators increasingly combine heterogeneous compute units, hierarchical memories, local buffers, and specialized data movement paths. This diversity makes fixed accelerator simulators difficult to extend beyond their original execution model. We present SCALE-Sim EVA, an extensible, visualizable, and adaptable simulation framework for IR-aware accelerator modeling. EVA represents workloads as tensor-based commands, tracks runtime tensor placement and readiness, and executes commands on composable hardware components with user-defined functional units and memory behavior. Instead of replaying address-level cycle traces, EVA computes cycle timing from tensor readiness, hardware-unit availability, and modeled operation or transfer latency. Its command decomposition mechanism bridges compiler-level IR granularity and hardware-level execution granularity, allowing global tensor operations to be lowered and scheduled locally across a hardware hierarchy. EVA also emits open command and storage traces for visual analysis of execution timelines, memory occupancy, and resource contention.

</details>


### [34] [Lonic: Algorithm-Hardware Co-Design for Energy-Efficient Fully Local Online SNN Training with INT4 Precision](https://arxiv.org/abs/2608.12500)
*Peilin Chen,Xiaoxuan Yang*

Main category: cs.AR

TL;DR: 论文提出Lonic方案，通过算法硬件协同设计，在INT4低精度SNN在线训练算法与乘法-free硬件架构协同优化，实现比GPU和现有加速器提升17-66倍能效，同时保持精度。


<details>
  <summary>Details</summary>
Motivation: 现有SNN训练算法虽提升了能效优势，但缺乏与实际芯片效率的验证。亟需一种能效和可扩展性兼备的解决方案，对齐算法优化与硬件特性。

Method: 算法层面：设计INT4低精度在线SNN训练算法，维持准确率。硬件层面：构建无乘法整数PE阵列、双优化零门控、时序前缀加速数据流、低精度权重传输。两者协同设计，实现硬件能效与算法计算的深度匹配。

Result: 相较于Apple M4和Nvidia V100 GPU，Lonic在能效上分别提升17.44倍和66.28倍，加速分别为3.25倍和1.02倍。相较于ASIC TPU-like和H2Learn加速器，能效提升15.95倍（14.64倍）和1.52倍（7.28倍），且面积效率显著提升。

Conclusion: Lonic通过算法硬件协同设计，实现SNN训练的能效和扩展性双重突破，证明了低精度算法优势可有效转化为实际芯片效益，为后续高效神经形态计算提供可行方案。

Abstract: Spiking neural networks (SNNs) have recently attracted increasing attention as an energy-efficient learning paradigm. Existing works also propose temporally and fully local online SNN training algorithms to address memory and computation overhead. However, they do not consider whether the algorithmic advantages can be effectively translated into real-device efficiency. To address this challenge, we present Lonic, an algorithm-hardware co-design for energy-efficient and scalable fully local online supervised SNN learning. On the algorithm side, we implement an INT4 low-precision training algorithm for fully local online SNN learning while maintaining accuracy. On the hardware side, to leverage the benefits of the proposed algorithm, we introduce reconfigurable multiplier-free integer PE arrays, dual-optimization zero-gating strategy, temporal prefix-accelerated local learning dataflow, and low-precision weight movement to significantly improve training efficiency. Compared to Apple M4 and Nvidia V100 GPUs, Lonic achieves average energy efficiency improvements of 17.44x and 66.28x, respectively, along with speedups of 3.25x and 1.02x, respectively. Moreover, Lonic achieves 15.95x (14.64x) and 1.52x (7.28x) energy efficiency (area efficiency) over ASIC TPU-like and H2Learn accelerators, respectively. The code for Lonic is available at https://github.com/peilin-chen/Lonic.

</details>


### [35] [GateTruth: Auditing the Rigor of RTL Design Benchmarks via Mutation Testing](https://arxiv.org/abs/2608.12635)
*Meet Bhadra*

Main category: cs.AR

TL;DR: 本文提出GateTruth，一种基于突变测试的RTL基准审计引擎，旨在通过注入语义突变并测量测试基准捕获率来量化测试基准的可信度，揭示现有基准的局限性并指出Token长度限制对模型评估的影响。


<details>
  <summary>Details</summary>
Motivation: 现有RTL大语言模型评估基准虽增多，但未采用突变测试这一成熟的硬件验证技术来检验测试基准是否真正刺激了被测逻辑。一个永不失败的测试基准不能作为正确性的证据，可能仅因未触及被破坏的逻辑。

Method: 本文引入GateTruth突变测试引擎和方法论，向参考设计注入确定性种子语义突变，并通过固定的合成到时序流程在可重复执行下测量测试基准杀伤率。在68任务的双轨参考套件上验证，并将同一引擎应用于RTLLM v2.0与CVDP基准，揭示了公开基准缺失参考解的结构性阻碍，以及4096-token输出上限对模型排名的静默截断影响。

Result: 在自家68任务套件中，60 Track A测试基准中有46个在顺序、可重复执行下杀伤至少95%的注入突变，其余14个因Goodhart效应等原因未达标。在RTLLM v2.0上，46个可审计设计中有72%低于95%阈值，三个直接得分0%。此外，4096-token上限 silently 截断了三个模型，扩展至16,384 tokens后有一模型从第五名升至第一。

Conclusion: 论文论证突变杀伤认证应成为RTL生成基准的标准报告要求，并呼吁在基准报告中统一采用突变测试以确保测试基准的可信度，同时警惕输出长度限制对评估结果的潜在偏差。

Abstract: Benchmarks for evaluating large language models on register-transfer-level (RTL) hardware design have proliferated rapidly, yet none reports having applied mutation testing, an established hardware-verification technique for quantifying testbench quality, to ask whether its own testbenches are trustworthy. A testbench that never fails is not evidence of a correct design; it may simply never stimulate the logic that is actually broken. We introduce GateTruth, a mutation-testing engine and methodology for auditing RTL benchmark testbench rigor: inject a deterministic, seeded set of semantic mutants into a reference design and measure what fraction the testbench catches.
  We validate the methodology against our own 68-task, dual-track reference suite -- 60 specification-to-RTL generation tasks and 8 agentic-repair tasks, scored through a pinned, deterministic synthesis-to-timing flow with correctness enforced as a strict gate -- certifying that 46 of 60 Track A testbenches kill at least 95% of injected mutants under sequential, reproducible execution; we disclose why the other 14 do not, including a Goodhart effect on testbenches revised to pass this gate. We then point the same engine, unmodified, at RTLLM v2.0, a widely adopted external benchmark: of 46 auditable designs, 72% fall below the 95% floor our own suite is held to, and three score 0% outright.
  A comparable audit of NVIDIA's CVDP benchmark is structurally impossible: its public release withholds reference solutions, removing the golden RTL mutation testing requires. Auditing our own instrument also surfaced a second finding: an initially uniform 4096-token output cap silently truncated three of seven evaluated models, and re-running at 16,384 tokens moved one model from fifth place to first. We argue mutation-kill certification should become a standard reporting requirement for RTL-generation benchmarks generally.

</details>


### [36] [Spec-Driven Hardware Evolution via Executable Contract Refinement and Proof-Guided RTL Update](https://arxiv.org/abs/2608.12684)
*Shibo Zhao,Yang Zhang,Mengxia Tao,Baoqi Zhang,Kezhi Li,Qiang Xu,Binwu Zhu,Hao Yan,Min Li*

Main category: cs.AR

TL;DR: This paper introduces 'spec-driven hardware evolution', a framework for evolving hardware designs (RTL) using executable contracts that define required behavior changes while preserving legacy implementations. It emphasizes semantic versioning over full RTL regeneration.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based hardware research focuses on prompt-to-RTL generation rather than managing semantic evolution of trusted legacy designs. Hardware development is naturally evolutionary (iterative changes), but current methods lack support for incrementally improving validated designs through versioned changes.

Method: The framework defines four stages: 1) Specify (create reviewed executable contracts for changes), 2) Plan (analyze RT-level deltas and affected RTL regions), 3) Implement (legacy-aware RTL updates under proof-guided checking), 4) Validate (iterative repair and verification). It shifts focus from RTL generation to spec-guided semantic evolution while preserving working implementations.

Result: Successful evaluation showed feasibility of contract-driven evolution and effective RTL update workflow. The system achieved validated convergence with legacy RTL toward new functionality under reviewed contracts, supporting incremental improvement rather than full regeneration.

Conclusion: Spec-driven evolution enables systematic semantic versioning of hardware designs, preserving verified implementations while incorporating changes through a validated pipeline of specification, planning, implementation, and verification stages.

Abstract: Hardware development is inherently evolutionary: major revisions typically begin by changing intended behavior and then updating a previously validated implementation, rather than regenerating RTL from scratch. Yet most recent LLM-based hardware research still frames the task primarily as prompt-to-RTL generation, offering limited support for semantic version evolution of trusted legacy designs. We present spec-driven hardware evolution, a contract-centered formulation for RTL version iteration. Instead of treating a new feature request as a direct prompt for RTL generation, we refine it into a reviewed executable contract for the next version. This contract specifies what must hold at the externally visible transactional level through a behavior-level reference together with explicit observation and checking semantics, while leaving how the change is realized in RTL to the evolution process. Based on this formulation, we organize hardware evolution into four stages: Specify, Plan, Implement, and Validate. After contract approval, the remaining stages proceed automatically: Plan derives cross-version semantic deltas and localizes affected RTL regions, aided by mutation-based semantic probing; Implement and Validate then perform legacy-aware RTL update under proof-guided checking and iterative repair. We evaluate the framework on a controlled version-evolution case study of a representative TPU datapath block under data-format changes. The results support the feasibility of contract-driven hardware evolution and demonstrate that the proposed backend workflow can effectively drive validated legacy RTL toward next-version functional convergence under a reviewed executable contract. An anonymous artifact for reproducibility is available at https://anonymous.4open.science/r/SDHE-3A6C.

</details>


### [37] [SynAct: A Reasoning-Acting Large Language Model Agent for Adaptive Synthesis Optimization](https://arxiv.org/abs/2608.12751)
*Fangzhou Liu,Peiyi Han,Jiawei Liu,Yuan Pu,Zhuolun He,Rongliang Fu,Tsung-Yi Ho,Bei Yu*

Main category: cs.AR

TL;DR: SynAct is an adaptive closed-loop LLM reasoning-acting agent that improves logic synthesis by iteratively diagnosing live synthesis reports and issuing targeted optimization commands to reduce worst negative slack (WNS) while balancing area and power.


<details>
  <summary>Details</summary>
Motivation: Logic synthesis PPA results are highly sensitive to optimization command choices, making tuning high-dimensional and expensive. Existing approaches either use black-box automated search over fixed action spaces with limited interpretability, or LLM-based methods that generate static scripts upfront and cannot adapt to evolving circuit states.

Method: SynAct employs an adaptive closed-loop LLM reasoning-acting agent that iteratively diagnoses live synthesis reports, reasons over the current circuit state, retrieves tool knowledge, and leverages historical optimization experience to issue targeted commands focused on improving timing performance.

Result: Across 14 designs tested on a commercial synthesis tool, SynAct reduces average worst negative slack (WNS) to 27% of that obtained from bootstrap synthesis.

Conclusion: SynAct provides a significant improvement in timing optimization by combining LLM-driven reasoning with adaptive closed-loop control, addressing the limitations of previous automated and static LLM-based synthesis approaches.

Abstract: Logic synthesis transforms RTL designs into gate-level netlists, where PPA results are highly sensitive to the choice of optimization commands, making synthesis tuning both high-dimensional and expensive. Previous approaches fall into two categories: automated methods, which perform black-box search over fixed action spaces with limited decision-level interpretability, and LLM-based methods, which typically generate static scripts upfront and cannot adapt to evolving circuit states. We present SynAct, an adaptive closed-loop LLM reasoning--acting agent that iteratively diagnoses live synthesis reports and reasons over the current circuit state, retrieved tool knowledge, and historical optimization experience to issue targeted commands. SynAct focuses on improving timing, particularly worst negative slack (WNS), while maintaining balanced area and power trade-offs. Experiments on a commercial synthesis tool across 14 designs show that SynAct reduces average WNS to 27% of that from bootstrap synthesis.

</details>


### [38] [Dryas: A Reprogrammable Engine for High-Speed Interconnect Tracing and Analysis](https://arxiv.org/abs/2608.12934)
*Manuel Bröchin,Tom Kuchler,Michael Giardino,David Cock,Timothy Roscoe*

Main category: cs.AR

TL;DR: Dryas 是一款开源 FPGA 工具，利用非确定性有限自动机（NFA）在缓存行粒度下实时过滤高速互连的跟踪数据，支持在不中断应用的情况下快速调试和性能分析。


<details>
  <summary>Details</summary>
Motivation: 异构系统的互连复杂且高带宽低延迟，调试与优化需处理数百 GB 的跟踪数据，传统方法成本高、效率低。

Method: 在 FPGA 上实现可运行时可重编程的覆盖层引擎，使用状态转换元件（STE）高效实现 NFA；过滤器可在不到 1 秒内切换，无需重新编程。

Result: 在 30 GiB/s、200 ns 的高速互连上验证了 Dryas 能捕获稀有、瞬态事件；对不同规模 NFA 的可扩展性进行了评估，并在调试互连实现和缓存行为分析两种场景中取得实效。

Conclusion: Dryas 为实时互连分析提供了低资源、灵活且高效的解决方案，显著提升了 FPGA 互连调试与加速应用性能调优的效率。

Abstract: The proliferation of heterogeneous components in modern computing systems has been accompanied by new higher bandwidth and lower latency interconnects. These interfaces and protocols are enormously complex and the process of developing, debugging, and analyzing FPGA-based implementations requires significant engineering work. Moreover, once a functional implementation is completed, optimization of the controller and associated software requires processing potentially hundreds of gigabytes of trace data.
  In this paper, we present Dryas, an open source tool for analyzing such an interconnect. We developed our tool, using minimal hardware resources, alongside an FPGA implementation of a very high speed, low latency (30~GiB/s, 200~ns) interconnect. With our run-time reprogrammable overlay engine we can inspect this interconnect to find rare, complex, or transient events even at full operation. This filtering engine is based on non-deterministic finite automata (NFAs), efficiently implemented using state transition elements (STEs), allowing us to trace events at a cache-line granularity. Moreover we can change the filters in less than a second, without reprogramming the FPGA or interfering with the running application. This data enables not only debugging the implementation of the interconnect itself, but analyzing the behavior of accelerated applications.
  We examine the mathematical basis for using NFAs and describe their implementation on a real coherent CPU-FPGA research platform. We then evaluate the scalability of Dryas for various size NFAs, followed by two different use cases: debugging FPGA implementation of the interconnect and analyzing cache behavior.

</details>


### [39] [Why Do Prefetchers Fail? Let Agents Answer](https://arxiv.org/abs/2608.13027)
*Xiangfeng Sun,Ceyu Xu,Ningzhi Ai,Zeyu Zhu,Yiyang Yuan,Yuan Xie*

Main category: cs.AR

TL;DR: 提出了一种基于性能异常驱动的自动研究流程，构建混合预取器（MoP），在SPEC CPU2006和SPEC CPU2017上实现了61.1%的几何平均IPC加速，优于人类设计的Alecto、Berti和Pythia预取器，证明了智能代理驱动的硬件设计可以产生实用的RTL级别预取器。


<details>
  <summary>Details</summary>
Motivation: 传统硬件预取器设计依赖人工专家对执行和内存访问轨迹进行手动分析，这既耗时又难以系统性地处理数十亿指令的真实工作负载，导致设计缺乏可靠改进保证。

Method: 采用性能异常驱动的自适应研究流，迭代地诊断部署预取器失败原因，将诊断结果用于构建混合预取器（MoP）。每个迭代将高影响力未解释的缺失定位到程序计数器，并为代理提供硬件日志、源代码和切片轨迹，验证诊断通过可运行最小案例，并综合构建针对重复模式族的专用子预取器。剩余异常反馈回迭代，实现超越模型先验的仿真器内循环发现。

Result: 在SPEC CPU2006和SPEC CPU2017上，MoP实现了相较无预取的61.1%几何平均IPC加速，在SPEC CPU2006上比Alecto快14.5%，在SPEC CPU2017上比Berti快21.6%，比Pythia快23.6%。RTL综合显示需要110KB片上存储和0.0347mm²面积。这是首次经验证智能代理驱动的硬件设计过程能够产生在未见过的工作负载上优于最先进的人类设计的RTL可行预取器。

Conclusion: 该研究首次展示了智能代理驱动的硬件设计流程能够生成实用的RTL级别预取器，在多个基准测试中显著优于人类设计的预取器，证明了数据驱动和自动化的硬件优化方法具有潜力。

Abstract: Hardware prefetchers are crucial to processor performance, yet their design remains labor-intensive and expert-driven. Architects inspect execution and memory-access traces, identify patterns, translate them into online hardware heuristics, and evaluate them in simulation, often with no guarantee of improvement. Human experts cannot systematically inspect billion-instruction traces across diverse real-world workloads.
  We present a performance-anomaly-driven autoresearch flow that repeatedly asks why a deployed prefetcher fails and uses the diagnoses to construct the Mixture of Prefetchers (MoP). Each iteration localizes high-impact unexplained misses to program counters, gives agents hardware logs, source code, and sliced traces, validates diagnoses through runnable minimal cases, and synthesizes specialized sub-prefetchers for recurring pattern families. Measured performance and remaining anomalies feed subsequent iterations, enabling simulator-in-the-loop discovery beyond model priors.
  The campaign consumes 1.91 billion DeepSeek V4 Pro tokens. On SPEC CPU2006 and SPEC CPU2017, MoP achieves a 61.1% geomean IPC speedup over no prefetching, outperforming the human-designed Alecto, Berti, and Pythia prefetchers by 14.5%, 21.6%, and 23.6%, respectively. RTL synthesis in a 6nm library reports 110 KB of on-chip storage and 0.0347 mm^2 area. To our knowledge, this is the first empirical demonstration that an agent-driven hardware-design process can produce an RTL-practical prefetcher that outperforms state-of-the-art human designs on unseen workloads.

</details>


### [40] [Potential Applications of HBF in LLM Serving Systems](https://arxiv.org/abs/2608.13127)
*Yihan Yin,Yinlun Zhao,Zhixin Yun,Guanying Wu,Feng Zhu,Kai Tao,Shu Li,Fei Huang,Zhe Zhang,Shuangchen Li,Hongzhong Zheng*

Main category: cs.AR

TL;DR: 探讨使用高带宽闪存（HBF）作为现有HBM推理系统的容量扩展方案，以缓解LLM推理中的内存容量限制问题。


<details>
  <summary>Details</summary>
Motivation: 随着模型权重、KV缓存及多模型变体规模的增长，LLM推理正面临日益严峻的内存容量挑战。

Method: 将HBF集成到GPU内存层级中，并构建系统级模型，利用其扩展只读模型状态对象的驻留容量，同时确保不损害计算核心所需的带宽。

Result: 通过模拟验证，HBF可以增加MoE模型的专家副本数量，并减少多模型推理时的模型加载时间并支持热模型复制。

Conclusion: 在保持HBM执行路径带宽的同时，利用HBF扩展模型权重驻留集，能显著提升大规模推理系统的服务能力。

Abstract: LLM serving is increasingly constrained by memory capacity as model weights, KV caches, and the number of served model variants continue to grow. This report examines High-Bandwidth Flash (HBF) as a capacity-oriented extension to HBM-based serving systems. We first discuss how HBF can be integrated into the GPU memory hierarchy without undermining the bandwidth expected by the compute die. We then model the system-level value of added capacity as expanded residency for read-mostly model-state objects. Under this view, HBF can improve MoE serving by enabling more expert replicas and can improve multi-model serving by reducing model loading and supporting hot-model replication. Our simulation results show that these benefits depend on preserving the HBM-resident execution path while using HBF to expand the resident set of model weights.

</details>


### [41] [ROLoad-PMP: Securing Sensitive Operations for Kernels and Bare-Metal Firmware](https://arxiv.org/abs/2608.13287)
*Wende Tan,Chenyang Li,Yangyu Chen,Yuan Li,Chao Zhang,Jianping Wu*

Main category: cs.AR

TL;DR: 本文提出了一种名为 ROLoad-PMP 的轻量级硬件-软件协同设计方案，通过新增只读内存加载指令和编译时内存布局优化，保护低层软件（如内核和固件）中的敏感操作免受控制流劫持攻击。


<details>
  <summary>Details</summary>
Motivation: 现有解决方案普遍仅保护部分目标，且性能开销较大，不适用于资源受限的系统（如物联网设备）或底层软件（如内核和裸机固件）。

Method: 首先设计了一组新指令，仅允许从带有特定密钥的只读内存区域加载数据，从而确保 potentially 被破坏的数据指针所指向内容的完整性；其次提出程序固化机制，在编译时将敏感操作的操作数分类并放置到不同密钥的只读内存中，并在运行时使用 ROLoad-PMP 系列指令进行加载。

Result: 在基于 RISC-V 的 FPGA 原型上实现，并以前向边缘控制流完整性防御为例进行验证。结果表明，ROLoad-PMP 仅需不到 1.40% 的额外硬件资源；同时支持多种轻量级防护机制（如性能开销可忽略不计，小于 0.853%），并提供比现有硬件方案（如 ARM BTI 和 Intel CET）更广泛和强大的安全保障。

Conclusion: ROLoad-PMP 方案以低硬件开销和微小性能影响有效提升了低层软件的安全性，特别适用于资源受限环境，是一种实用的防控控制流劫持攻击的新方法。

Abstract: A common way for attackers to compromise victim systems is hijacking sensitive operations (e.g., control-flow transfers) with attacker-controlled inputs. Existing solutions in general only protect parts of these targets and have high performance overheads, which are impractical and hard to deploy on systems with limited resources (e.g., IoT devices) or for low-level software like kernels and bare-metal firmware. In this paper, we present a lightweight hardware-software co-design solution ROLoad-PMP to protect sensitive operations from being hijacked for low-level software. First, we propose new instructions, which only load data from read-only memory regions with specific keys, to guarantee the integrity of pointees pointed by (potentially corrupted) data pointers. Then, we provide a program hardening mechanism to protect sensitive operations, by classifying and placing their operands into read-only memory with different keys at compile-time and loading them with ROLoad-PMP-family instructions at runtime. We have implemented an FPGA-based prototype of ROLoad-PMP based on RISC-V, and demonstrated an important defense application, i.e., forward-edge control-flow integrity. Results showed that ROLoad-PMP only costs few extra hardware resources (< 1.40%). Moreover, it enables many lightweight (e.g., with negligible overheads < 0.853%) defenses, and provides broader and stronger security guarantees than existing hardware solutions, e.g., ARM BTI and Intel CET.

</details>


### [42] [YAVIN: A Unified Architecture for Secure Edge Processing in Memory](https://arxiv.org/abs/2608.13496)
*Shouzhi Fang,William C. Tegge,Md Omar Faruque,Peipei Zhou,Endadul Hoque,Alex K. Jones*

Main category: cs.AR

TL;DR: YAVIN是一个统一的可信计算基(TCB)，将TEE扩展至内存，支持处理器和内存中处理(PIM)的可信执行，解决了边缘计算中多租户安全执行的挑战。


<details>
  <summary>Details</summary>
Motivation: 现有TEE仅保护处理器内数据，无法在内存中直接进行可信计算；PIM虽能缓解von Neumann瓶颈，但缺乏安全机制。需要一种统一TCB，跨越处理器、内存和加速器实现安全私密的多租户执行。

Method: 利用传统TEE建立的受保护内存区域，YAVIN让数据在TEE内由处理器或PIM解密、处理、重新加密。首次在PIM上实现LightSaber KEM抗量子密码系统和ASCON-128认证加密，协同设计算法以高效在DRAM执行，建立并维护共享加密状态。针对张量工作负载，通过密码学-PIM协同设计重组计算，满足认证加密的顺序约束，同时采用位切片排序限制临时明文暴露。

Result: 相比最新PIM AES实现，YAVIN实现20倍以上加速；执行INT8和INT32量化边缘级LLM时，分别仅带来34%和9.3%的开销，相对于明文执行。

Conclusion: YAVIN通过将TEE扩展至内存并结合PIM协同设计，实现了高效、安全的跨处理器和内存的可信执行，为边缘计算多租户场景提供了实用的安全解决方案。

Abstract: Secure, private multi-tenant execution spanning processors, memory, and accelerators remains one of the most significant challenges in modern edge computing systems. Simultaneously, processing-in-memory (PIM) has emerged as an effective approach for reducing the Von Neumann bottleneck by moving computation closer to data. Existing trusted execution environments (TEEs) establish trust only within the processor, protecting data while it traverses untrusted resources such as the memory bus. Consequently, trusted computation cannot be performed directly within memory. We present YAVIN, a unified trusted computing base (TCB) that extends the TEE beyond the processor to encompass both processor execution and a dedicated memory region supporting trusted processing-in-memory execution while treating the memory bus as untrusted. Leveraging the dedicated protected memory regions already established by conventional TEE architectures, YAVIN enables data to be decrypted, processed, and re-encrypted by either processor or PIM execution while remaining within the TEE. To realize this unified TCB, YAVIN presents the first PIM implementations of the LightSaber KEM post-quantum cryptosystem and ASCON-128 authenticated encryption, co-designing both algorithms for efficient DRAM execution to establish and maintain shared cryptographic state. Finally, we demonstrate how cryptography-PIM co-design for tensor-based workloads reorganizes computation to satisfy the ordering constraints imposed by authenticated encryption with minimal performance overhead while simultaneously enabling bit-sliced ordering that limits temporary plaintext exposure. Compared to the latest PIM AES implementation, YAVIN achieves more than a 20x speedup while incurring only 34% and 9.3% overhead when executing INT8 and INT32 quantized edge-class LLMs, respectively, relative to plaintext execution.

</details>
